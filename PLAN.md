# План доработки курса

Рабочий документ для следующих сессий. Задаёт, что осталось сделать в
`lectures/` и `practices/`, в каком порядке и по каким источникам. Основан
на разборе `REPORT.md` и `SOURCE-REPORT.md` (пути в § 2).

Общий контекст репозитория — в [CLAUDE.md](CLAUDE.md). Как собирать —
в [lectures/README.md](lectures/README.md). Здесь только план работ.

**Целевой облик репозитория** (сформулирован заказчиком курса 15.08.2026):

1. лекции в формате Typst со скриптами сборки — **есть**;
2. практическая часть отдельно: примеры кода для лекций, практический проект
   к каждой лекции и один крупный сквозной — **есть**, см. § 5.4;
3. отдельная лекция по языку **Takt** с примерами — **есть** (лекция 11), см. § 6.11;
4. всё объединено в один CMake-проект, одной сборкой собираются и PDF лекций,
   и примеры — **есть**, см. § 3.1;
5. в лекциях шрифт Fira Code — **есть**: с 16.08.2026 им набирается весь
   текст лекции, а не только листинги (`body-in-mono: true` в
   `template/theme.typ`);
6. примеры кода с подсветкой синтаксиса — **есть** (`template/code.typ`);
7. все примеры и проекты на C90 со строгой проверкой — **есть**, см. § 3.1;
8. проверка code-style в конвейере CI/CD — **есть**, см. § 3.1.

---

## 0. Как пользоваться

1. Прочитать § 3 «Текущее состояние» — понять, что уже сделано.
2. Выбрать очередь работ из § 8 или конкретную лекцию из § 6.
3. Перед правкой лекции прочитать соответствующий раздел `REPORT.md` § 6
   (структура лекции) и § 5 (примеры) — здесь они пересказаны, но в
   первоисточнике подробнее.
4. После правки — прогнать проверку из § 10.
5. Отметить сделанное прямо в этом файле (снять пункт, дописать дату).

**Правило:** ненаписанное помечается блоком `#todo[...]`, а не молчаливым
пропуском. Блок виден в PDF красной врезкой; `show-todo: false` в
`lectures/course.typ` убирает все такие врезки перед раздачей студентам.

---

## 1. Быстрый старт для новой сессии

Весь курс одной сборкой — PDF лекций и примеры практикума:

```bash
cd /Volumes/HOME/GitHub/statecraft
cmake -S . -B build           # typst + компилятор C
cmake --build build -j        # PDF в build/lectures/, бинарники примеров
ctest --test-dir build        # тесты примеров
cmake --build build --target style-check
```

Работа только над лекциями — быстрее через `make`. CMake вызывает те же цели,
поэтому логика сборки не раздваивается, а лекции собираются и без CMake:

```bash
cd lectures
make fix                 # окружение: typst, шрифты, пути, пробная сборка
make                     # все лекции в out/ + копии под читаемыми именами
make watch L=04-dfa-nfa  # пересборка одной лекции при правке
make sync-check          # листинги лекций против practices/
grep -rn '#todo\[' src/  # что не написано
```

Требуется: `typst` ≥ 0.15, `cmake` ≥ 3.16, компилятор C; `pandoc` — только для
`tex2typ.sh`, `clang-format` — только для полной проверки оформления.
Пакеты `@preview` (fletcher, CeTZ) прогреваются `./scripts/vendor-packages.sh`.

---

## 2. Где что лежит

| Что | Путь |
|---|---|
| Лекции (рабочая копия) | `lectures/src/<id>/main.typ`, `main.body.typ` |
| Шаблон | `lectures/template/` |
| Метаданные и реестр лекций | `lectures/course.typ` — единственное место правки |
| Общая нотация, глоссарий, вопросы, задачник | `lectures/shared/` |
| Библиография | `lectures/bib/references.bib` |
| Сборка лекций из CMake | `lectures/CMakeLists.txt` (реестр читается из `course.typ`) |
| Практикум на C90 | `practices/` |
| Общий режим компиляции C90 | `cmake/StatecraftStrictC.cmake` |
| Совместимость с C90 (`bool`, `int32_t`) | `practices/common/base_types.h` |
| Проверка оформления кода | `.clang-format`, `scripts/check-style.sh` |
| CI | `.github/workflows/ci.yml` |
| **Разбор ошибок и план курса** | `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ/REPORT.md` |
| **Разбор LaTeX-исходников** | `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ/SOURCE-REPORT.md` |
| **Корпус источников, 402 файла** | `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ/docs/` |
| Каталог корпуса с аннотациями | `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ/DOCS.md` |
| Оригиналы лекций на LaTeX | `/Volumes/HOME/GitHub/articles/` |

Корень репозитория — `/Volumes/HOME/GitHub/statecraft` (в прежних редакциях
плана стоял несуществующий путь `~/github/statecraft`).

Карта «какой источник корпуса к какой лекции» — в § 11 этого файла и в
`DOCS.md`, часть IV.

---

## 3. Текущее состояние

| # | id | Тема | Готовность | Что это значит |
|---|---|---|---|---|
| 1 | `01-intro` | Введение, алфавиты, слова, языки | 90 % | История, формальная часть и организационный раздел (17.08.2026): что знать заранее, формы контроля, порядок сдачи лабораторных, требования к коду |
| 2 | `02-fsm` | Конечный автомат, Мили и Мур | 85 % | Перенос + преобразование Мили ⇄ Мур, детектор `1101`, врезка о фон Неймане, турникет и светофор как первые примеры; все диаграммы векторные (16.08.2026) |
| 3 | `03-synthesis` | Синтез и автоматные схемы | 85 % | Перенос + все таблицы, карты Карно и схемы векторизованы, формулы проверены минимизацией |
| 4 | `04-dfa-nfa` | ДКА и НКА | 75 % | Определения, алгоритмы и четыре полных разбора (16.08.2026): два примера прежних лекций в нотации акцепторов, детерминизация и минимизация $(a\|b)^*abb$, теорема Мура, экспоненциальный взрыв. Осталась векторизация части диаграмм |
| 5 | `05-kleene` | Регулярные события, теорема Клини | 75 % | Перенос + теорема + лемма о накачке + нерегулярность скобочных последовательностей с переходом к магазинной памяти (17.08.2026). Нет леммы Ардена и метода исключения состояний |
| 6 | `06-regexp` | Регулярные выражения, лексический анализ | 90 % | Перенос + PCRE/ReDoS с измерениями, пять типовых выражений, разбор учебного ДКА, лабораторная «лексер CSV двумя способами» (17.08.2026) |
| 7 | `07-automata-prog` | Автоматное программирование | 85 % | Перенос + листинги + границы применимости + три реализации одного автомата и тестирование автоматных программ с практикой `07-three-ways` (16.08.2026) |
| 8 | `08-statecharts` | Иерархические автоматы, кодогенерация | 85 % | Теория написана 17.08.2026: statechart стиральной машины с числами, SFC против диаграммы состояний, два слоя форматов и генераторов, автомат TCP, разбор выхлопа SimInTech. Блоков `#todo` не осталось |
| 9 | `09-verification` | Верификация и тестирование | 85 % | Написана 17.08.2026: автоматы Бюхи и сведение живости к достижимости, взрыв пространства состояний и приёмы борьбы, разбор всех примеров Promela, лабораторная со светофором и LTL, что теряется при переводе кода в модель. Блоков `#todo` не осталось |
| 10 | `10-limits` | Границы модели, МТ, вычислимость | 90 % | Перенос `МТ` + машина Поста + приложения. 16.08.2026: гёделева нумерация, универсальная машина, диагонализация, «Энигма». 17.08.2026: МП-автоматы, формальная сложность (P, NP, сведение, Кук — Левин), теорема Райса, колмогоровская сложность, композиция машин с переходником, языки без полноты по Тьюрингу, итог курса. Блоков `#todo` не осталось |
| 11 | `11-cells` | Клеточные автоматы и самовоспроизведение | 90 % | Выделена из лекции 10. Классификация конфигураций (16.08.2026); 17.08.2026 — нумерация правил Вольфрама, классы поведения, тьюринг-полнота правила 110, муравей Лэнгтона, раздел реализации с практикой `practices/11-cells` |
| 12 | `12-takt` | Язык Takt | 95 % | Написана 16.08.2026; 17.08.2026 добавлены формат сценария симулятора, параметризация и импорт, сравнение с Ragel/SCXML/UML/QP/UniMod |

Проверено 16.08.2026: `check.sh` проходит, `build.sh` собирает все 11 PDF за
~2 с, блоков `#todo` — 25. Оценки готовности в таблице — экспертные, по объёму
написанного текста, а не измеренные.

### 3.1. Инфраструктура сборки — сделано 16.08.2026

Одна сборка на весь курс: корневой `CMakeLists.txt` подключает `practices`
(примеры) и `lectures` (PDF через typst). Реестр лекций CMake читает из
`course.typ` тем же способом, что и `scripts/lib.sh`, — источник правды не
раздваивается. Цели: `lectures`, `lecture-<id>`, `style-check`, `style-fix`;
тесты примеров зарегистрированы в `ctest`.

Что было и что стало:

| Было | Стало |
|---|---|
| `cmake_minimum_required(VERSION 3.2.1)` — CMake 4.x отказывается конфигурировать | 3.16 в корне и во всех подпроектах |
| строгие флаги записаны в несуществующую переменную `CMAKE_CFLAGS` — **не применялись вовсе** | `statecraft_strict_c()`: `-std=c90 -pedantic -Wall -Wextra -Wshadow -Wstrict-prototypes …`, при `STATECRAFT_STRICT=ON` (по умолчанию) — `-Werror` |
| `07-fsm-insert-element` не собирался (нет `size_t`), Catch2 v2.3.0 не компилировался на arm64 | собирается всё, 5/5 тестов проходят на macOS arm64 |
| `<stdbool.h>`/`<stdint.h>` в 15 файлах — заголовки C99 | `common/base_types.h`: те же имена средствами C90, штатные заголовки подключаются, если компилятор C99/C++ |
| 114 предупреждений компилятора | 0 |
| требования к оформлению кода — только текстом в `_1.CodeStyle` (каталог удалён 16.08.2026) | `.clang-format` + `scripts/check-style.sh` (табуляция, длина строки, `//`, BOM, CRLF, пробелы в конце) |
| CI нет | `.github/workflows/ci.yml`: оформление, практикум на ubuntu и macOS, лекции с выкладкой PDF |

Исправленные при этом дефекты кода: неинициализированное состояние автомата в
`07-simple-program` (UB при каждом запуске), чтение датчиков D3–D5 в
`03-control-program/engine.c`, результат которого нигде не использовался,
`strdup` (POSIX, не C90) в лексере, отсутствие проверок `calloc`, `%llu` для
`size_t` в `07-fsm-insert-element`, `system("pause")` и `return EXIT_FAILURE`
при успешном завершении в `10-turing-machine/main.c`.

### 3.2. Реорганизация практикума — сделано 16.08.2026

`_2.Practice/` → `practices/`; каталоги переименованы в kebab-case и получили
номер лекции, к которой относится практика (`02-fsm-delay`,
`07-fsm-insert-element`, `10-turing-machine`, …) — соответствие «практика ↔
лекция» теперь видно из имени каталога, а не из таблицы в документации.
Общая часть разделена: `common/` — исходный код (`base_types.h`,
`base_macro.h`, точка входа тестов, `network/`, заголовок Catch2),
`resources/` — сторонние ресурсы (сборки SPIN под Windows). Состав и
соответствие лекциям — в `practices/README.md`. Имена целей CMake и тестов
приведены к тому же виду (`10-turing-machine-tests`).

Каталог `_3.Documents/` (21 PDF, 11 МБ) удалён: те же работы есть в корпусе
`docs/` каталога АПСУ.

`.clang-format` сведён с документом `_1.CodeStyle/-1.CodeStyle.md` по пунктам; весь код
практикума переформатирован (31 файл). Единственное расхождение документа с
самим собой — § «Отступы»: правило требует четырёх пробелов, а примеры в
документе набраны двумя; принято правило, примеры в документе стоит поправить.

Осталось по инфраструктуре:

- [ ] прогнать `clang-format` по всему практикуму (`scripts/check-style.sh --fix`)
      и закоммитить отдельным коммитом: локально clang-format не установлен,
      поэтому проверка форматирования пока идёт только в CI;
- [ ] `practices/09-promela` не подключён к сборке — нужен отдельный режим
      (проверка синтаксиса `spin -a`), см. § 5.8;
- [ ] `11.FSM_ControlPneumo/generated/*.inc` и `*.log` — в CP1251; перекодировать
      в UTF-8 при переносе в лекцию 8 (копии в `lectures/src/08-statecharts/`
      уже перекодированы);
- [ ] в `generated/PneumoAutomate.h` вшит абсолютный путь
      `E:\GitHub\automata_programming\...` — вычистить.

**Нумерация провизорная.** Она сведена из трёх источников и подлежит
согласованию с преподавателем. Перенумерация — правка реестра в
`lectures/course.typ` плюс `git mv` каталога в `src/`. `REPORT.md` § 6.2
предлагает альтернативный, прикладной порядок — если курс переориентируется
на разработку ПО, брать его.

---

## 4. Замечания REPORT.md: что закрыто

### Закрыто

| Код | Суть | Где |
|---|---|---|
| К-1 | Убрано «при помощи КА можно описать любой алгоритм», написан раздел «что добавляет лента» + таблица иерархии Хомского | `10-limits`, `sec:tm-vs-fsm` |
| К-2 | Снежинка Коха больше не пример невычислимости; вместо неё busy beaver и колмогоровская сложность | `10-limits` |
| К-3 | Разведены факторизация (RSA) и дискретный логарифм (Диффи — Хеллман) | `10-limits` |
| К-4 | Канонические уравнения разменного аппарата: `mod 3` и `⌊⌋` | `03-synthesis` |
| К-5 | Определение регулярного выражения отделено от определения события | `05-kleene` |
| К-6 | `M⁺` названа итерацией | `05-kleene` |
| С-1 | Таблица МТ для `y = x+1` дополнена столбцом `q₂` + два примера прогона | `10-limits`, приложение А |
| С-2 | Индекс `j` в формуле команды МТ; «в состояние q₁» | `10-limits` |
| С-4 | Тезис Чёрча — Тьюринга отделён от физического и расширенного | `10-limits` |
| С-6 | `2ⁿ` в правиле Фредкина — восстановился сам при пересборке (в LaTeX было верно, терялось в PDF) | `10-limits/cells` |
| С-7 | Вставлены листинги 1 и 2 из практикума | `07-automata-prog` |
| С-8 | Добавлена формулировка теоремы Клини с разбором, какая лемма даёт какое направление | `05-kleene` |
| С-9 | Ссылки на леммы указывают на правильные леммы (в LaTeX было верно, ломалось в PDF) | `05-kleene` |
| С-10 | Разделены определения функций переходов и выходов в лемме № 8 | `05-kleene` |
| С-11 | Пример ревнивой квантификации переписан на `a*a` против `a*+a` | `06-regexp` |
| С-12 | «Мили», не «Миля» | `02-fsm` |
| С-13 | «B — алфавит выходных символов» | `02-fsm` |
| § 4.1 | Диаграммы, таблицы и схемы лекции 3 переведены в вектор | `03-synthesis` |
| § 4.2 | Текст извлекается и ищется: typst встраивает шрифты корректно | все |
| § 4.5 | Титул с версией и датой, единый шаблон | `template/` |
| § 4.7 | Ссылки печатаются с видимым URL или сноской | частично |

Сверх списка REPORT.md найдено и исправлено при переносе:

- пять итоговых формул синтеза схемы не попадали в PDF (`multiline` вместо
  `multline`), рисунок «Анализ автомата акцептора» разваливался, две системы
  кодировок не выводились (два `cases` в одном `equation`);
- **формула φ₂ синтеза разменного аппарата неверна**: четвёртая конъюнкция и
  свёрнутая форма расходятся с таблицей переходов. Исправлено минимизацией
  Квайна — Мак-Класки, проверено на всех двенадцати достижимых наборах;
- выходные функции обозначались вариантным начертанием той же буквы, что и
  функции переходов, и на письме не отличались; теперь `φ` — переходы,
  `ψ` — выходы.

### Закрыто 16.08.2026 (очередь 1)

| Код | Что сделано | Где |
|---|---|---|
| С-3 | Написан раздел «Нумерация машин и универсальная машина»: гёделева нумерация, счётность машин против несчётности функций, теорема о существовании универсальной МТ с доказательством-интерпретатором. Проблема останова переписана: функция $h$, теорема Тьюринга, доказательство самоприменением, объяснение диагонали и приём сведения | `10-limits`, `sec:goedel` |
| С-5 | Раздел «Взлом Энигмы» переписан: устройство машины, инволюция без неподвижных точек, cribs как метод, Реевский — Ружицкий — Зыгальский с `bomba kryptologiczna` и листами Зыгальского, бомба Тьюринга — Уэлчмана с диагональной доской. В библиографию добавлены `Rejewski_1981`, `Welchman_HutSix`, `Turing_1936` | `10-limits` |
| § 2.3 | Противоречие о положении головки МТ снято: в § «Определение» принято соглашение источника (крайняя левая буква), добавлено примечание о том, что положение головки — часть условия задачи, со ссылкой на приложение | `10-limits` |
| § 2.3 | Фрагмент Гарднера сшит: убрана речь от первого лица («в моей книге»), добавлена врезка об источнике, введено определение скорости света $c$, на которое ссылается разбор глайдера | `11-cells` |

Проверено при этом же проходе и оказалось уже исправленным: опечатки § 2.3,
имена Бэнкса и Фредкина, число плиток Eternity, описание `^`/`$` и таблица
метасимволов `\w`/`\W`, пояснение к `y = x·2`.

### Не закрыто

| Код | Суть | Куда | Оценка |
|---|---|---|---|
| § 3.3 | Лекция 7 подана как одна статья 2000 г. без рамки — рамка добавлена, но нужен разбор по современным источникам | `07-automata-prog` | § 6 |

---

## 5. Сквозные работы

Не привязаны к одной лекции; делать параллельно.

### 5.1. Единая нотация и глоссарий — §§ 3.4, 7.15

Файлы: `lectures/shared/notation.typ`, `lectures/shared/glossary.typ`.

Сделано: зафиксированы `q₀` (начальное), `F` (заключительные), `ε` (пустое
слово), пятёрки/шестёрки автоматов, обозначения LTL. В глоссарии 14 терминов
с привязкой к лекциям.

Осталось:

- [ ] пройти по всем десяти лекциям и привести обозначения к
      `shared/notation.typ`; сейчас перенесённый текст местами сохраняет
      исходные `A`/`B` вместо `Σ`/`Y`;
- [ ] добавить в глоссарий таблицу соответствия терминов Кузнецова и терминов
      теории автоматов («локальное событие» = буква входного алфавита,
      «модификация» = действие) — REPORT.md § 3.4, последний абзац;
- [ ] включить глоссарий во все лекции (`glossary: true`) или оформить
      отдельным приложением к курсу;
- [ ] в `10-limits` оставлено локальное отступление: там `q₁` — начальное,
      `q₀` — заключительное, по источнику Марченкова. Стоит либо переписать
      таблицы под нотацию курса, либо оставить примечание (сейчас примечание).

### 5.2. Контрольные вопросы — § 7.20

Файл: `lectures/shared/questions.typ`. По 5–7 вопросов на каждую из
десяти лекций уже написано. Осталось:

- [ ] сверить вопросы с финальным содержанием лекций после доработки —
      часть вопросов сейчас относится к ещё не написанным разделам;
- [ ] добавить к каждому вопросу отсылку к разделу лекции.

### 5.3. Задачник — § 7.21

Файл: `lectures/shared/tasks.typ`. Есть 3–5 задач на лекцию с уровнями
сложности. Требование REPORT.md — 10–15 на лекцию **с ответами**.

- [ ] довести до 10–15 задач на лекцию;
- [ ] добавить ответы и критерии проверки (сейчас ни того, ни другого);
- [ ] решить, печатать ответы в той же лекции или отдельным документом
      (предложение: отдельный `src/answers/`, собирается опционально).

### 5.4. Практические проекты: по одному к каждой лекции плюс сквозной

Требование заказчика: у каждой лекции — свой практический проект, и сверх
того один крупный, собирающий курс воедино. Всё живёт в `practices/` и
собирается общей сборкой; в лекции листинги подтягиваются через
`#code-file("/src/<id>/code/…")`, то есть код не дублируется, а копируется
скриптом из практикума (см. § 5.9).

Что уже есть и к какой лекции относится:

| Лекция | Проект в `practices/` | Состояние |
|---|---|---|
| 1 | `01-words` — слова и языки, операции над ними | есть, тесты есть |
| 2 | `02-fsm-delay` — автомат «задержка», Мили | есть, тесты есть |
| 3 | `03-synthesis` — синтез схемы, Квайн — Мак-Класки | есть, тесты есть |
| 3, 7, 8 | `03-control-program` — датчики, движок, эмуляция | прикладной пример |
| 4 | `04-dfa-nfa` — детерминизация и минимизация, вывод DOT | есть, тесты есть |
| 5 | `05-kleene` — конструкция Томпсона | есть, тесты есть |
| 6 | `06-regular-expression` — учебный движок РВ | есть, тесты есть |
| 6 | `06-lexical-analyze` — лексер математических выражений | есть, тесты есть |
| 7 | `07-simple-program` — контрпример на вложенных `switch`/`if` | есть |
| 7 | `07-fsm-insert-element` — автомат поиска вставки | есть, тесты есть |
| 7 | `07-three-ways` — шлагбаум тремя способами, покрытие переходов | есть, тесты есть |
| 8 | `08-fsm-control-pneumo` — циклограмма пневмоцилиндров, кодогенерация | есть |
| 9 | `09-promela` — 10 примеров Promela | есть, вне сборки (§ 5.8) |
| 10 | `10-turing-machine` — интерпретатор МТ | есть, тесты есть |
| 11 | `11-cells` — правила Вольфрама, «Жизнь», муравей Лэнгтона | есть, тесты есть |
| 11 | `12-takt` — модели на Takt, генерация C, драйвер | есть, тест есть |

Проекты есть у каждой лекции. Сделано 16.08.2026: `01-words` (лекция 1),
`03-synthesis` (лекция 3), `04-dfa-nfa` (лекция 4), `05-kleene` (лекция 5).
Практики лекций 5 и 4 соединяются в конвейер и дают сквозной пример курса:
`(a|b)*abb` → 14 состояний по Томпсону → 5 после детерминизации → 4 после
минимизации. Общее представление автоматов — `practices/common/fsm.[ch]`,
алгоритмы — в самих практиках.

Крупный сквозной проект — сделано 16.08.2026:

**`practices/20-welding-line`** — управление сварочной линией. Автоматная
модель задана таблицей переходов (лекция 3), реализована с отделением
ввода-вывода через HAL (лекция 7), выдержки и таймауты входят в модель
(лекция 8), считается покрытие переходов и порождается модель на Promela
(лекция 9). Прогон печатает трассу и непокрытые переходы; тесты набирают
100 % покрытия переходов набором прогонов. В README проекта — постановка,
разбор принятых решений и три задания на доработку (ручной режим, второй
пост сварки как ортогональный регион, свойство безопасности в LTL).

Описание каждой работы (постановка, порядок сдачи, критерии) готовить тем же
шаблоном typst в `lectures/src/lab-N/`; в `course.typ` их не заводить —
нужен отдельный реестр или ручная сборка.

### 5.8. Promela в общей сборке — сделано 16.08.2026

`scripts/check-promela.sh` разбирает все модели (`spin -a`), а с ключом
`--verify` собирает `pan` и прогоняет проверку. SPIN внешний: без него
скрипт печатает предупреждение и завершается успешно — так же, как проверки
`clang-format` и `taktc`. Цели CMake: `promela-check` и `promela-verify`;
в CI добавлено задание с установкой SPIN из пакетов ubuntu.

Ожидаемый исход модель заявляет сама строкой `verify: ok|violation|skip` в
шапке: для примеров, демонстрирующих ошибку (`05.WaitExpression`,
`07.ResourceExclude`), отсутствие нарушения — тоже ошибка.

Первый же прогон нашёл два дефекта в моделях, которые до этого никогда не
запускались: в `06.DoAndProduceConsume` цикл закрывался словом `do` вместо
`od` (модель не разбиралась вовсе), а в `08.MutexAlgorithm` выражение
`turn[i + 1]` при `i = 1` обращалось за границу массива из двух элементов.
Вторая модель переписана на алгоритм Петерсона и теперь действительно
доказывает взаимное исключение; упражнение в лекции 9 переформулировано под
неё.

### 5.9. Синхронизация кода лекций и практикума — сделано 16.08.2026

`lectures/scripts/sync-code.sh` копирует файлы практикума в
`lectures/src/<id>/code/` по манифесту `scripts/code-map.txt`; с ключом
`--check` (цель `make sync-check`, стоит в CI) он ничего не меняет, а
сравнивает и падает при расхождении.

Первый прогон нашёл, ради чего всё затевалось: **девятнадцать копий из
тридцати девяти разошлись с практикумом**. В лекции 2 показывался
`delay_fsm.c` с типом `InsertingEngine` из чужой практики — тот самый дефект
копипасты, который в практикуме уже исправлен, а в лекции остался. Остальные
расхождения — код до переформатирования `clang-format`.

Вне манифеста намеренно: `08-fsm-control-pneumo/generated/*.inc` (в CP1251,
копии в лекции перекодированы) и `12-takt/code/driver.c` (написан для лекции,
в практикуме исходника нет).

### 5.10. Единый вход в сборку лекций — сделано 16.08.2026

Логика сборки лекций была записана дважды: в `scripts/build.sh` (аргументы
typst, шрифты, кэш пакетов, параллельность) и в `lectures/CMakeLists.txt`
(свой `add_custom_command` на каждую лекцию). Теперь описание одно:
`lectures/Makefile` плюс `scripts/`, а `CMakeLists.txt` вызывает
`make pretty OUT=<build>/lectures` и держит только граф зависимостей, по
которому решает, звать ли `make`. Следствия:

- `cd lectures && make` собирает курс без CMake тем же кодом и в том же
  порядке; `make` по умолчанию — это `make pretty`, то есть PDF плюс копии
  под читаемыми именами;
- каталог результата переносится ключом `OUT=<каталог>` (в `build.sh` —
  `--out`), поэтому сборка из CMake кладёт PDF в `build/lectures`, а ручная —
  в `lectures/out`;
- цели CMake: `lectures`, `lecture-<id>`, `lectures-check`,
  `lectures-sync-check`, `lectures-clean` — все делегируют в `make`.

### 5.5. Организационный раздел курса — § 3.5

- [ ] входные требования (дискретная математика, булева алгебра, C) —
      в лекцию 1;
- [ ] форма контроля, порядок приёма лабораторных — источник
      `Description.md` в корне репозитория;
- [ ] требования к оформлению кода собрать тем же шаблоном отдельным
      документом: форматирование задаёт `.clang-format`, остальные правила
      (именование, объявление переменных, `switch`) — `practices/README.md`;
- [ ] единый список литературы курса — частично сделан в
      `bib/references.bib` (добавлены Хопкрофт — Мотвани — Ульман, Сипсер,
      «Дракон», Шалыто, Поликарпова — Шалыто, Samek, Holzmann,
      Baier — Katoen); осталось напечатать его отдельным разделом
      «Что читать дальше» в лекции 10.

### 5.6. Современные примеры вместо монет — § 7.24

Разменный аппарат на монетах 1/3/5/10 копеек — единственный прикладной
пример курса, причём номиналов нет в обращении 35 лет.

- [ ] лекция 2: турникет (2 состояния), светофор с кнопкой пешехода,
      антидребезг кнопки, детектор `1101`;
- [ ] лекция 3: счётчик по модулю k, лифт на 3 этажа, шлагбаум с концевиками
      и фотобарьером; разменный аппарат оставить, но добавить вариант
      с номиналами 1/2/5/10 ₽;
- [ ] лекция 6: лексер CSV с кавычками (RFC 4180, 4 состояния), декодер UTF-8
      как ДКА (9 состояний, таблица Бьорна Хёрмана);
- [ ] лекция 8: автомат TCP из RFC 793 (11 состояний).

Полный список с обоснованием — `REPORT.md` § 5.1.

### 5.7. Векторизация оставшейся растровой графики — § 4.1

Сделано: все таблицы переходов, десять карт Карно, диаграмма Мура разменного
аппарата, четыре структурные схемы.

Осталось (все — диаграммы Мура из примеров, задают условие задачи):

- [x] `02-fsm/images/KA_Moore_Automata_*.png` — 4 диаграммы: сделано
      16.08.2026, растровые файлы удалены;
- [x] `03-synthesis/images/KA_Moore_AnalyzeAutomata_*.png` — 3 диаграммы:
      сделано 17.08.2026, растровые файлы удалены;
- [x] `05-kleene/images/RE_*.png` — 4 диаграммы: сделано 17.08.2026, включая
      пять шаблонов конструкции Томпсона на CeTZ; растровые файлы удалены;
- [ ] `10-limits/cells/images/Life_*.png` — конфигурации игры «Жизнь»,
      их удобнее рисовать генератором клеточного поля, а не вручную.

Инструмент: `fsm-diagram` из `template/diagrams.typ` (fletcher) для диаграмм
состояний, `scheme` (CeTZ) для схем. Разбор ошибок при перерисовке — § 9.

---

## 6. Работы по лекциям

Формат: что есть → что дописать → откуда брать → чем проверить.

### Лекция 1 — `01-intro`. Введение, алфавиты, слова, языки

Есть: история программирования и языка Си (перенос `00.History.md`),
определения алфавита, слова, конкатенации, языка, свойства алгоритма,
сквозной пример «турникет».

Дописать:

- [ ] организационный раздел (§ 5.5 этого плана) — сейчас `#todo`;
- [ ] «место дисциплины»: от реле и логических схем к моделям вычислений,
      три «этажа» курса — сейчас есть кратко, стоит развернуть;
- [ ] показать турникет не только словами: диаграмма состояний прямо в
      лекции 1, чтобы студент в первый день увидел, к чему всё идёт;
- [ ] префикс, суффикс, подслово — в определениях их нет.

Источники корпуса: `ifmo-automata/_autonemi_3.pdf` (популярный вход),
`ifmo-works/_paradigma_automata.pdf` § «Введение»,
`web/computer-museum-avtomatnoe-programmirovanie-obzor.pdf` (история).

Проверка: в PDF должны быть заполнены разделы, соответствующие пустым
`КА` § 1.1 «Определения» и `МТ` § 1 «Введение».

---

### Лекция 2 — `02-fsm`. Конечный автомат, модели Мили и Мура

Есть: определение автомата, примеры «задержка», дизъюнктор, сумматор,
сумматор по модулю 2 (сильная часть исходника, сохранена), диаграмма Мура,
различие Мили и Мура, две векторные структурные схемы.

Дописать:

- [x] **взаимное преобразование Мили ⇄ Мур в обе стороны** — сделано
      16.08.2026: теорема о равносильности с оценкой |Q|·|B| + 1, обе
      конструкции, объяснение платы за преобразование в каждую сторону;
- [x] **детектор последовательности `1101`** — сделано 16.08.2026: построен
      в обеих моделях векторными диаграммами, сравнение сведено таблицей
      (4 состояния против 5, момент выдачи выхода, откуда он снимается);
- [x] турникет и светофор с кнопкой пешехода как первые примеры — сделано
      16.08.2026 (раздел «Автомат вокруг нас», обе диаграммы векторные);
- [x] врезка о фон Неймане — сделано 16.08.2026;
- [x] векторизовать 4 диаграммы Мура — сделано 16.08.2026; растровые
      оригиналы удалены.

Источники корпуса: `ifmo-automata/_klimovich_solovyev.pdf` (преобразование
Мили → Мур с количественной оценкой), `ifmo-books/_book.pdf` гл. 1,
`web/wiki-en-mealy-machine.pdf`, `web/wiki-en-moore-machine.pdf`.

Задание для студентов: детектор фронта и антидребезг кнопки (уже в задачнике).

---

### Лекция 3 — `03-synthesis`. Синтез автоматов и автоматные схемы

Есть: разбор разменного аппарата целиком, кодирование алфавитов, таблицы
переходов, десять карт Карно, все пять формул синтеза (φ₂ исправлена),
автоматная схема до и после перекодировки, анализ поведения с тремя
примерами. Всё, кроме трёх диаграмм Мура из примеров анализа, векторно.

Дописать:

- [ ] **метод последовательного наращивания состояний** — в исходнике он
      изложен в лекции по автоматному программированию (`АП` § 2.4), но
      относится сюда. Перенести приём, оставив в лекции 7 применение;
- [ ] связь с синтезом цифровых устройств: комбинационная часть плюс
      триггеры — сейчас упомянуто, стоит развернуть;
- [ ] счётчик по модулю k как продолжение примера MOD2 из лекции 2;
- [ ] современные примеры (§ 5.6): лифт, шлагбаум;
- [ ] векторизовать 3 диаграммы Мура из раздела «Анализ поведения».

Источники корпуса: `ifmo-books/A008.pdf`, `A009.pdf` (граф-схемы, Шалыто),
`ifmo-automata/_2011_01_03_shalyto-tukkel.pdf` (переход от схемы алгоритма к
графу переходов), `ifmo-automata/_metod065.pdf`, `_s7300.pdf` (ПЛК).

Лабораторная 1 (§ 5.4).

---

### Лекция 4 — `04-dfa-nfa`. Автоматы-акцепторы, ДКА и НКА

**Самая крупная дыра исходных материалов** (REPORT.md § 6, лекция 4).
Написана заново, но пока это каркас.

Есть: определение ДКА с `F`, таблица соответствия нотаций, определение НКА и
ε-НКА, формулировка теоремы о детерминизации со схемой доказательства,
алгоритм subset construction, достижимость, определение эквивалентности,
теорема Майхилла — Нероуда, алгоритм разбиения.

Дописано 16.08.2026 (блоков `#todo` не осталось):

- [x] разбор примеров-акцепторов `{00, 001, 101}` и `{010, 11}` **в обеих
      нотациях**: перевод Мили → акцептор с расщеплением состояний, цена
      перевода в состояниях (5 → 6 в обоих случаях), врезка о мусорном
      состоянии. Оба автомата набраны в формате практикума и прогнаны
      программой — минимизация не склеивает ни одной пары;
- [x] **полный разбор детерминизации** `(a|b)*abb`: разбор ε-НКА Томпсона по
      фрагментам, ε-замыкание начального состояния, таблица подмножеств с
      расшифровкой каждого, пошаговый расчёт одной строки, диаграмма ДКА;
- [x] **полный разбор минимизации** того же автомата: три уточнения разбиения
      с таблицами и различающими словами, проверка устойчивости, сверка с
      автоматом, выписанным в начале лекции «из головы»;
- [x] теорема Мура о различимости: определение `k`-эквивалентности,
      доказательство оценки `n − 2` через монотонность разбиений, пример
      достижимости оценки на счётчике по модулю `n`;
- [x] практическое обоснование минимизации: память прошивки, число тестов на
      покрытие переходов, сравнение автоматов, диагностика проектирования —
      плюс врезка о том, когда минимизировать не надо;
- [x] пример экспоненциального взрыва: язык «`n`-й символ с конца равен `a`»,
      доказательство `2ⁿ` классов по Майхиллу — Нероуду, замечание о ленивом
      построении ДКА в `re2` и `rust regex`;
- [ ] векторизовать оставшиеся диаграммы примеров (акцепторы разобранных
      языков нарисованы через `fsm-diagram`, растровых в лекции нет).

Источники корпуса: `ifmo-automata/vashkevich-nda.pdf` (учебное пособие по
недетерминированным автоматам), `web/wiki-en-powerset-construction.pdf`,
`web/wiki-en-dfa-minimization.pdf`,
`ifmo-works/akishev-dvorkin-2010-full.pdf` (минимальные ДКА для префиксных
кодов). Теория — Хопкрофт — Мотвани — Ульман (`@Hopcroft` в библиографии).

Лабораторная 2 (§ 5.4).

---

### Лекция 5 — `05-kleene`. Регулярные события и теорема Клини

Есть: определения событий и операций, определение регулярного выражения
(отделено от события), формулировка теоремы Клини, три леммы, примечание о
соответствии «обобщённый источник = ε-НКА» и «лемма 7 = конструкция
Томпсона», лемма о накачке с доказательством нерегулярности `aⁿbⁿ`.

Исправлено 16.08.2026: в разборе примера ${010, 11}$ переходы из состояния
`q₂` были перепутаны местами — по описанию автомат уходил в «мусорное»
состояние на слове `010`, то есть не принимал слово, ради которого строился.
Диаграмма Мура была верной; расхождение вскрылось, когда автомат набрали в
формате практикума и прогнали через `04-dfa-nfa`. **Приём годится для всей
проверки лекций: любой разобранный вручную автомат стоит прогонять
программой.**

Дописать:

- [ ] **лемма Ардена и метод исключения состояний** — направление
      «автомат → регулярное выражение» сейчас доказано неконструктивно.
      Именно эти два инструмента дают алгоритм (REPORT.md § 6, лекция 5);
- [ ] сквозной пример `(a|b)*abb`: конструкция Томпсона здесь, дальше
      детерминизация и минимизация в лекции 4 — связать явными ссылками;
- [ ] второй пример на лемму о накачке: правильные скобочные
      последовательности (мотивация МП-автоматов в лекции 10) — сейчас `#todo`;
- [ ] язык двоичных записей чисел, кратных 3 — три состояния, показывает,
      что «арифметика» иногда влезает в конечную память;
- [ ] алгебра событий: выписать тождества таблицей, сейчас они разбросаны;
- [ ] векторизовать 4 диаграммы.

Источники корпуса: `web/wiki-en-thompson-construction.pdf`,
`web/wiki-en-kleene-algorithm.pdf`, `web/wiki-en-pumping-lemma.pdf`.

---

### Лекция 6 — `06-regexp`. Регулярные выражения и лексический анализ

Есть: история, синтаксис, жадность/ленивость/ревнивость (пример исправлен),
раздел «PCRE ≠ регулярные языки», ReDoS, два семейства движков, BNF
разбираемого языка, интерфейсы лексера и учебного движка.

Дописать:

- [ ] расширить историю: Клини (1951) → Томпсон в `ed`/`grep` → POSIX →
      Perl (Уолл, 1987) → PCRE. Сейчас только про Perl;
- [ ] уточнить `^`/`$`: начало и конец **строки**, не текста; флаг `m`,
      якоря `\A`/`\z` (§ 2.3 замечаний);
- [ ] восстановить обрезанную таблицу метасимволов `\w`/`\W` (§ 2.3);
- [ ] замер времени ReDoS: таблица для `^(a+)+$` на входах длины
      10, 20, 25, 30 в Python `re` и в `rust regex` — сейчас `#todo`;
- [ ] разбор реализации `simple_regexp.c` как прямой трансляции определения
      совпадения; сравнение с построением ДКА — сейчас `#todo`;
- [ ] лексер CSV по RFC 4180: 4 состояния, весь стандарт;
- [ ] декодер UTF-8 как ДКА (9 состояний, таблица Хёрмана);
- [ ] `flex` / `re2c` / `Ragel`: показать генерацию ДКА в виде C-кода.

Источники корпуса: `web/Cox-regular-expression-matching-can-be-simple-and-fast.pdf`
(**обязательное чтение**), `web/wiki-en-redos.pdf`, `web/wiki-en-ragel.pdf`,
`ifmo-works/11-Paraschenko-Stankevich.pdf`, `12-Paraschenko-Stankevich.pdf`.

Лабораторная 3 (§ 5.4).

---

### Лекция 7 — `07-automata-prog`. Автоматное программирование

Есть: изложение метода по Кузнецову, разбор примера поиска вставок с рабочими
листингами из практикума, 18 пунктов критики, раздел «когда подход оправдан»
с таблицей границ применимости и контрпримером.

Дописано 16.08.2026 (блоков `#todo` не осталось):

- [x] **три реализации одного автомата** — раздел написан на примере
      шлагбаума: `switch`, таблица переходов с указателями на функции, паттерн
      State в C90. Сравнение по объёму кода, месту хранения модели, стоимости
      добавления состояния и события, цене подсчёта покрытия. Код —
      `practices/07-three-ways/`, тесты сверяют трассы трёх реализаций;
- [x] **тестирование автоматных программ** — покрытие состояний, переходов и
      путей; теорема о недостижимости покрытия путей при наличии цикла;
      измеренное покрытие набором сценариев (ни один сценарий не даёт больше
      35 %, весь набор — 100 %); показано, что 100 % покрытия переходов
      уживается с дефектом в расширенном состоянии; W-метод, установочные и
      диагностические последовательности со ссылкой на теорему Мура из
      лекции 4;
- [ ] таблица соответствия терминов Кузнецова и теории автоматов (§ 5.1);
- [ ] уточнить формулировку про «произвольный циклический алгоритм»: речь об
      управляющем автомате при конечном множестве состояний управления,
      иначе противоречие с лекцией 10;
- [ ] операционализировать требование ортогональности: как его проверять
      таблицей, а не на глаз;
- [ ] вложенные автоматы, вызов автомата из автомата;
- [ ] Мили и Мур в коде: действия на переходах против действий в состояниях.

Источники корпуса: `ifmo-books/_book.pdf` гл. 2–3, `ifmo-books/_umk.pdf`,
`ifmo-science/MS_Tests_CLR.pdf` (switch против паттерна State против языка),
`ifmo-disser/stepanov_disser.pdf` (методы реализации),
`ifmo-progeny/kuznetsov-shalyto.pdf` (полемика — прямо к этой лекции),
`ifmo-automata/_autmicroc.pdf`, `_autmicroc2.pdf` (микроконтроллеры),
`web/wiki-en-state-pattern.pdf`. (Каталог `_3.Documents/` с локальными копиями
удалён 16.08.2026; те же работы есть в корпусе `docs/`.)

Лабораторная 5 (§ 5.4).

---

### Лекция 8 — `08-statecharts`. Иерархические автоматы и кодогенерация

Есть: постановка про комбинаторный взрыв, перечень средств statecharts,
раздел про цикл ПЛК и SFC, полный разбор практики — циклограмма
пневмоцилиндров с таймаутами и выдержками, состав выхлопа кодогенератора
SimInTech, две ловушки сгенерированного кода.

Дописано 17.08.2026 (блоков `#todo` не осталось, 12 страниц → 20):

- [x] **statechart стиральной машины** — составное состояние цикла, история,
      ортогональный регион индикации, переход по аварии из границы составного
      состояния; сравнение с плоским автоматом по числам (37 состояний и 185
      клеток против 11 и 13);
- [x] SFC против диаграммы состояний — таблица соответствия элементов с
      колонкой расхождений, три существенных отличия (множество активных
      шагов, отсутствие произвольной вложенности, вычисление условий каждый
      цикл ПЛК);
- [x] обзор форматов и генераторов в два слоя: форматы хранения графа (DOT,
      TGF, GML, GraphML, GXL, XGML/GrUML, GreQL — по черновику из `articles`)
      и средства с семантикой автомата (SCXML, UML SMD, Stateflow, Ragel,
      QP/QM, SimInTech, Takt) с колонкой «куда встраивается ручной код»;
- [x] **автомат TCP** из RFC 793 — таблица одиннадцати состояний, две
      векторные диаграммы (установление и закрытие соединения), разбор
      `TIME-WAIT`, `CLOSING` и `RST` как перехода из границы;
- [x] разбор выхлопа кодогенератора — таблица восьми блоков схемы против
      порождённого кода, граница между генератором и ручным кодом, шаг
      интегрирования как такт автомата, хеш схемы, порядок совместной отладки
      `automate.prt` и `model.prt`;
- [ ] пошаговая настройка генератора по скринкасту не пересказана намеренно:
      последовательность нажатий меняется от версии к версии, в лекции дана
      ссылка на `video/Установка_и_настройка_кодогенератора.mkv`.

Источники корпуса: `web/wiki-en-uml-state-machine.pdf`,
`web/statecharts-dev-what-is-a-statechart.pdf`,
`web/wiki-en-sequential-function-chart.pdf`, `web/wiki-en-iec-61131-3.pdf`,
`web/wiki-en-scxml.pdf`, `ifmo-disser/gurov_disser.pdf` (UniMod),
`external/1-s2.0-0167642387900359-main.pdf` (оригинальная статья Харела),
`external/Samek-state-pattern-UltimateHook.pdf`.

---

### Лекция 9 — `09-verification`. Верификация и тестирование

Есть: различие тестирования и верификации, критерии покрытия, список
статических проверок автоматного описания, таблица операторов LTL с примерами
требований, порядок работы со SPIN, таблица десяти примеров Promela из
практикума, разбор примера взаимного исключения с упражнением.

Дописано 17.08.2026 (блоков `#todo` не осталось, 10 страниц → 17):

- [x] **W-метод** — метод изложен в лекции 7, здесь дана ссылка и таблица
      «тестирование против верификации» с их слепыми зонами: дублировать
      материал не нужно;
- [x] **автоматы Бюхи** — определение, четыре шага сведения живости к
      достижимости (модель как автомат, отрицание свойства в `never claim`,
      произведение, проверка пустоты), объяснение `acceptance cycle`;
- [x] взрыв пространства состояний — оценка $n^k$ и пять приёмов борьбы
      (частичные порядки, символьная проверка, абстракция с CEGAR,
      ограничение глубины, `bitstate hashing`) с указанием, что теряет каждый;
- [x] разбор всех примеров Promela — по одному наблюдению на пример, с
      объяснением, зачем конструкция нужна верификатору;
- [x] **лабораторная**: автомат светофора из лекции 2 переписан на Promela
      (`practices/09-promela/10.TrafficLight.pml`), записаны два свойства на
      LTL, показана роль слабой справедливости: `liveness` нарушается без
      ключа `-f` и выполняется с ним. Три задания на доработку модели;
- [x] **верификация автоматных программ** — три способа получить модель
      (руками, порождением из кода, общим источником для кода и модели),
      таблица того, что теряется при переводе (условия на данные, арифметика,
      время, память, железо), и как после этого читать ответ верификатора.

Сверх плана: `scripts/check-promela.sh` научился проверять LTL-свойства —
модель с `ltl имя { … }` собирается без `-DSAFETY` и прогоняется по свойству
за запуск (`pan -a -f -N имя`).

Источники корпуса: `ifmo-verification/velder_verification_posobie.pdf`,
`ifmo-verification/_LTL_for_Spin.pdf`, `ifmo-verification/_model_checking.pdf`,
`ifmo-books/_principles_of_model_checking.pdf`,
`ifmo-books/_model-based_testing_of_reactive_systems.pdf`,
`ifmo-present/20120218_model_checking_karpov_lecture01/02.pdf`.

---

### Лекция 10 — `10-limits`. Границы модели, машина Тьюринга, вычислимость

**Разделена 16.08.2026.** Было 104 страницы при среднем по курсу около
тридцати — фактически две лекции под одной обложкой. Клеточные автоматы,
самовоспроизведение и игра «Жизнь» вынесены в лекцию 11 (`11-cells`), Takt
получил номер 12. После разделения: 72 страницы в лекции 10 (из них 20 —
приложение с задачами) и 36 в лекции 11. Пункты про клеточные автоматы из
списка ниже относятся теперь к лекции 11.

Есть: определение МТ, работа машины, композиция и итерация, тезис Чёрча —
Тьюринга с разделением на три варианта, раздел «что добавляет лента» с
таблицей иерархии Хомского, проблема останова, busy beaver, колмогоровская
сложность, две трудные задачи криптографии, полнота по Тьюрингу, клеточные
автоматы, игра «Жизнь», приложения с программами МТ.

Дописано 17.08.2026 (блоков `#todo` не осталось, 39 страниц → 49):

- [x] **МП-автомат** — определение семёркой, разбор $a^n b^n$ и скобочных
      последовательностей, таблица «что распознаёт каждая модель», почему
      недетерминизм здесь не бесплатен, связь с лексическим и синтаксическим
      анализом и вывод для автоматного программирования (вложенность требует
      стека или иерархических состояний);
- [x] **сложность формально** — O-нотация, `P`, `NP` через сертификат,
      полиномиальное сведение, NP-полнота, теорема Кука — Левина со схемой
      доказательства, связь с жадным покрытием импликант из лекции 3;
- [x] **теорема Райса** — определение нетривиального свойства, доказательство
      сведением к останову, разграничение «свойства функции» и «свойства
      текста», три законных выхода для статических анализаторов;
- [x] **колмогоровская сложность** — определение, несжимаемые строки,
      невычислимость через парадокс Берри, следствия для архиваторов и
      генераторов случайных чисел; лабораторная с перебором $B B (2)$;
- [x] приложение «Примеры композиций машин Тьюринга» — машина удвоения,
      разбор того, что определение композиции согласует состояния, но не
      положение головки, машина-переходник и готовая композиция для
      $y = 2 (x + 1)$ с потактовым прогоном;
- [x] раздел «Языки, у которых полноты нет намеренно» — IEC 61131-3 и цикл
      ПЛК, eBPF, языки конфигурации, языки описания автоматов; связка с тем,
      что ограниченность автомата и есть причина его выбора;
- [x] **итог курса** — карта пройденного по группам лекций и «что читать
      дальше» по трём направлениям со ссылками на библиографию;
- [ ] универсальная МТ и гёделева нумерация — сделано 16.08.2026;
- [ ] алгоритм Шора и что он означает для расширенного тезиса — не написан;
- [ ] распознавание $a^n b^n$ и палиндромов на МТ, копирование $w arrow.r w w$
      — не написаны (для $a^n b^n$ теперь есть разбор на МП-автомате).

Источники корпуса: `web/wiki-en-chomsky-hierarchy.pdf`,
`web/wiki-en-turing-machine.pdf`, `web/wiki-en-halting-problem.pdf`,
`web/wiki-en-busy-beaver.pdf`, `web/wiki-en-rule-110.pdf`,
`web/wiki-en-langtons-ant.pdf`, `web/wiki-en-game-of-life.pdf`,
`ifmo-papers/_ln_Thesis.pdf`, `ifmo-disser/ulyantsev-dissertation.pdf`
(сведение к SAT — практический сюжет о NP).

Лабораторная 4 (§ 5.4).

---

### Лекция 11 — `12-takt`. Язык Takt — сделано 16.08.2026

Язык определён заказчиком: [Takt](https://github.com/BasePractice/BuT)
(Typed, Automata, Known Timing) — DSL описания конечных автоматов, компилятор
`taktc` на Rust, цели генерации `c`, `c-hal`, `st`, `st-at`, `rust`, `sv`,
`sv-mmio`, `plantuml`, симулятор `takt-sim`, проверка LTL-свойств
`taktc verify`.

Написано: 32 страницы — зачем нужен отдельный синтаксис (связка с лекциями 7 и
8), модель исполнения и такт, типы и отображение в Си, порты и `extern fn`,
композиция `|` и `+`, приём «состояние обязано удерживать управление» с
контрпримером, разбор порождённого Си, драйвер, симулятор как эталон,
верификация и сравнение со SPIN из лекции 9, диагностика, границы
применимости. Каждый фрагмент кода скомпилирован и прогнан; модели лежат в
`practices/12-takt/model/` и собираются целью `12-takt-models`.

Дописано 17.08.2026:

- [x] формат файла сценария симулятора (`takt-sim -s scenario.json`): поля
      шага, именованная форма против позиционной, сценарий как тест модели;
      пример заведён в практикум (`scenario/watchdog.json`) и прогоняется
      в `ctest`;
- [x] сравнение с соседями по нише: Ragel, SCXML, UML/Stateflow, QP/QM,
      UniMod — по тому, что является входом и что проверяется до запуска;
- [x] параметризованные модели (`parameter`) и импорт (`import`);
- [ ] место лекции в курсе: тематически она примыкает к лекции 8
      (кодогенерация), номер 11 стоит потому, что нумерация ещё не утверждена.

**Расхождение симулятора и цели `c` при параллельной композиции.** Минимальный
пример: две модели, у каждой `always { x := x + 1; }`, корень
`start Root = A | B;`. Симулятор даёт 2, 4, 6 (тело каждой ветви исполняется
дважды за шаг), порождённый Си — 1, 2, 3. Одиночная модель без композиции
считает верно в обоих. Это противоречит объявленному в документации языка
статусу симулятора как эталона; сообщено автору языка. Пока расхождение не
устранено, в лекции нет потактовой трассы композиции — только структура
примера и вывод порождённого кода.

---

## 7. Что можно взять из репозитория, но пока не взято

| Источник | Куда | Состояние |
|---|---|---|
| `practices/03-control-program/` — датчики, движок, эмуляция по сети и из файла | прикладные примеры лекций 3, 7, 8 | не использован |
| `КР_15.11.2019.txt` — 30+ тем курсовых | варианты лабораторных, § 5.4 | не использован |
| `Description.md` — цели, требования, порядок приёма лабораторных | лекция 1, § 5.5 | не использован |
| `practices/CMakeLists.txt` + Catch2 | база для лабораторных с автопроверкой | не использован |
| `docs/version-control.md` — Git, 282 строки | вспомогательный документ курса | перенесён из `02.VerControlSystem` 16.08.2026 |
| `/Volumes/HOME/GitHub/articles/Языки задания конечных автоматов/` — 8 текстовых файлов | лекция 8, обзор форматов | не использован |
| `/Volumes/HOME/GitHub/articles/Конечные Автоматы/implementation_gl.tex` | лекция 10, приложение | не перенесён |

Старые md-лекции (`00.History` … `08.Promela_Spin`) разобраны и удалены
16.08.2026: история языка перенесена в лекцию 1 ещё раньше, задачи по машине
Тьюринга и раздел о машине Поста — в лекцию 10, типовые регулярные выражения
и пример НКА — в лекцию 6, окружение сборки — в `practices/README.md`,
справка по Git — в `docs/version-control.md`. Остальное (`06.Model_Checking`
на 3 байта, `05.Lexical_Analyze` на 12 строк BNF, команды graphviz из
`08.Promela_Spin`) уже присутствовало в лекциях или было пусто.

---

## 8. Очерёдность

Порядок выбран так, чтобы каждая очередь давала законченный результат.

### Очередь 0 — инфраструктура (сделано 16.08.2026)

Корневой CMake, строгий C90, code-style, CI — § 3.1. Остатки очереди:
`clang-format` по всему практикуму, Promela в сборке (§ 5.8), синхронизация
листингов (§ 5.9).

### Очередь 0.5 — лекция 11 по языку Takt (сделано 16.08.2026)

Лекция написана, примеры собираются (§ 6.11). Остались два блока `#todo`:
формат сценария симулятора и сравнение с Ragel/SCXML/UniMod/QP.

### Очередь 1 — остатки Приоритета 1 REPORT.md (сделано 16.08.2026)

С-3 и С-5 написаны, положение головки МТ согласовано, пересказ Гарднера сшит;
остальные пункты списка оказались уже исправленными. Разбор — в § 4.

### Очередь 2 — лекция 4 до рабочего состояния (сделано 16.08.2026)

Четыре блока `#todo` заменены полными разборами; сквозной пример
`(a|b)*abb` проходит через лекции 5 → 4 и сверяется с выводом практикума.
Диаграммы примеров лекции 5 векторизованы 17.08.2026.

### Очередь 3 — лекция 2 (сделано 16.08.2026)

Преобразование Мили ⇄ Мур, детектор `1101`, врезка о фон Неймане, турникет и
светофор с кнопкой пешехода как первые примеры до формализма, четыре
диаграммы Мура переведены в вектор (при перерисовке нашлась ошибка подписи на
диаграмме суммирующего автомата).

### Очередь 4 — лекция 7, три реализации и тестирование (сделано 16.08.2026)

Написаны оба раздела, заведена практика `practices/07-three-ways`
(четыре состояния, пять событий, три реализации, 8 тестов, подсчёт покрытия
переходов).

### Очередь 5 — лекции 8 и 9 (лекция 8 сделана 17.08.2026)

Осталась лекция 9: автоматы Бюхи, разбор девяти моделей Promela,
лабораторная на светофоре из лекции 2, получение модели Promela из
автоматного кода. Блок про W-метод закрывать не нужно — метод изложен в
лекции 7, достаточно ссылки.

### Очередь 6 — лекция 10 (сделано 17.08.2026)

МП-автоматы, формальная сложность, теорема Райса, колмогоровская сложность,
композиция машин Тьюринга, языки без полноты по Тьюрингу, итог курса.

### Найдено при прогоне актуальным `taktc`/`takt-sim` (17.08.2026)

Компилятор внешний и в CI курса не стоит, поэтому расхождения копились молча.
Прогон актуальной версией нашёл четыре:

- `watchdog.takt` и `button_led.takt` объявляли входные порты с начальным
  значением (`in kick: bit := 0`) — теперь это ошибка `SE-092`;
- `recovery.takt` сравнивал `bit` с целым (`error = 1`) — ошибка `SIM-005`;
- драйвер `practices/12-takt/main.c` использовал старые имена портов
  (`WATCHDOG_WATCHDOG_KICK` вместо `WATCHDOG_WATCHDOG_PORT_KICK`);
- драйвер устанавливал обработчики портов **после** `Watchdog_init`, а тот
  выполняет `enter` начального состояния и пишет в выходной порт, — segfault
  на первом же запуске.

Всё исправлено и проверено; вывод для плана — ТД-3 (CI) должен включать
задание с `taktc`, иначе история повторится.

### Очередь 7 — дидактическая обвязка (2–3 дня)

Задачник с ответами, описания практических работ, контрольные вопросы,
итог курса.

### Очередь 8 — недостающие практические проекты (§ 5.4)

Проекты к лекциям 1, 3, 4, 5 и крупный сквозной `20.Welding_Line`. Делать
после того, как соответствующая лекция доведена: проект пишется под текст,
а не наоборот.

### Очередь 9 — векторизация (сделано 17.08.2026)

Семь растровых диаграмм лекций 3 и 5 перерисованы: три диаграммы Мура
(`fsm-diagram`) и четыре рисунка регулярных событий, включая пять шаблонов
конструкции Томпсона на CeTZ. Растровые файлы удалены. Заодно обозначение
пустого слова в лекции 5 приведено к нотации курса: было $Lambda$, стало
$epsilon$.

Осталось по иллюстрациям: 21 МБ сканов книги Гарднера в лекции 11 — это
ТД-7 в `CLAUDE.md` (лицензия и качество); конфигурации «Жизни» следует
порождать практикой `practices/11-cells`, а не сканировать.

---

## 9. Грабли typst 0.15 и приёмы

Записаны по итогам работы; экономят время при следующих правках.

- Угловые скобки — только `chevron.l` / `chevron.r`. `angle.l` даёт
  «unknown symbol modifier».
- Единственный встроенный ГОСТ-стиль библиографии — `gost-r-705-2008-numeric`.
- Fira Code — variable-шрифт с начертанием по умолчанию Light (300); вес
  задан явно в `template/theme.typ`.
- Счётчик `figure` шагает по первому уровню и «схлопывает» двумерное
  значение, поэтому утверждения нумеруются сквозным номером по лекции.
- `read()` и `image()` внутри файлов шаблона разрешают относительные пути от
  самого шаблона: в `code-file` и `img` нужен абсолютный путь от корня
  (`/src/<id>/code/foo.c`). Внутри файла лекции обычный
  `image("images/foo.png")` работает как относительный.
- В подписи печатается полное слово («Рисунок 3 — …»), в ссылке —
  сокращение («на рис. 3»): иначе получается «приведена на Рисунок 3».
  Сделано show-правилом в `template/figures.typ`.
- В CeTZ общий `set-style(mark: ...)` навешивает наконечники на **все** пути
  холста, включая контуры фигур и транзитные линии. Стрелку задавать явно
  только на тех линиях, которые входят в элемент или уходят за пределы схемы.
- В fletcher `loop-angle` принимается только для петель; для обычных дуг
  значение `auto` роняет компиляцию. Петля без явного `bend` рисуется внутри
  узла и накрывает подпись — задавать `bend: 130deg`.
- `typst eval` требует, чтобы пробный файл лежал внутри `--root`.

### После конвертации из LaTeX проверять отдельно

Эти дефекты уже встречались и могут повториться при переносе новых файлов
через `scripts/tex2typ.sh`:

- вызовы `image(...)` теряют решётку и печатаются как текст;
- `&` из LaTeX превращается в `and` внутри `cases(...)`, создавая пустые
  строки в фигурных скобках;
- метки `<fig:...>` привязываются к следующему заголовку вместо рисунка;
- уровни заголовков сдвигаются, если исходную лекцию разрезали на части;
- `\,` из LaTeX становится тонким пробелом typst вместо запятой.

---

## 10. Проверка

### Definition of done для лекции

- [ ] собирается без ошибок и предупреждений: `./scripts/build.sh <id>`;
- [ ] нет `#todo` либо каждый оставшийся осознан и записан в этот план;
- [ ] обозначения соответствуют `shared/notation.typ`;
- [ ] все утверждения оформлены блоками (`#definition`, `#theorem`, …), а не
      обычным текстом — тогда работают ссылки;
- [ ] у каждого рисунка, таблицы и листинга есть подпись и метка;
- [ ] новые термины добавлены в `shared/glossary.typ`;
- [ ] контрольные вопросы в `shared/questions.typ` покрывают содержание;
- [ ] в задачнике есть задачи по теме, с ответами;
- [ ] источники заимствования указаны врезкой `#sources[...]`;
- [ ] новые ссылки на литературу добавлены в `bib/references.bib`.

### Definition of done для примера или проекта практикума

- [ ] собирается в строгом режиме без единого предупреждения:
      `cmake --build build` при `STATECRAFT_STRICT=ON`;
- [ ] проходит `scripts/check-style.sh`;
- [ ] есть тесты, зарегистрированные в `ctest`, и они проходят;
- [ ] код на ISO C90: без `<stdbool.h>`/`<stdint.h>` (вместо них
      `base_types.h`), без `//`, без объявлений после операторов;
- [ ] на код ссылается лекция через `#code-file`, а не копия текста.

### Команды

```bash
cmake -S . -B build && cmake --build build -j   # лекции + примеры
ctest --test-dir build --output-on-failure      # тесты примеров
./scripts/check-style.sh                        # оформление кода
./scripts/check-style.sh --fix                  # переформатировать

cd lectures
./scripts/check.sh            # окружение, битые пути, все @key библиографии
./scripts/build.sh            # все лекции
grep -rn '#todo\[' src/       # что осталось
```

Визуальная проверка постранично:

```bash
typst compile --root . --package-cache-path .typst-packages \
  --format png --ppi 90 src/04-dfa-nfa/main.typ /tmp/p{n}.png
```

Проверка воспроизводимости на чужой машине (сборка только на шрифтах из
`fonts/` и встроенных в typst):

```bash
STRICT_FONTS=1 ./scripts/build.sh
```

### Что проверять глазами после крупной правки

- титул, колонтитулы, оглавление, списки рисунков и таблиц одинаковы во всех
  десяти лекциях;
- ссылки `@fig:`, `@tab:`, `@lst:` ведут туда, куда написано в тексте, и
  читаются грамматически;
- формулы, выведенные из таблиц, пересчитаны, а не перерисованы со скана.

---

## 11. Карта корпуса источников

Полный каталог с аннотациями — `DOCS.md` (8141 строка) в каталоге АПСУ.
402 файла, ~334 МБ. Ниже — соответствие плану лекций.

| Лекция | Основные источники из `docs/` |
|---|---|
| 1 | `ifmo-automata/_autonemi_3.pdf`, `web/wiki-ru-avtomatnoe-programmirovanie.pdf`, `ifmo-works/_paradigma_automata.pdf`, `web/computer-museum-avtomatnoe-programmirovanie-obzor.pdf`, `ifmo-automata/_dedinskij.pdf` |
| 2 | `ifmo-books/_book.pdf` гл. 1, `ifmo-automata/_klimovich_solovyev.pdf`, `web/wiki-en-mealy-machine.pdf`, `wiki-en-moore-machine.pdf`, `wiki-en-state-diagram.pdf` |
| 3 | `ifmo-books/A008.pdf`, `A009.pdf`, `ifmo-automata/_2011_01_03_shalyto-tukkel.pdf`, `_metod065.pdf`, `_s7300.pdf` |
| 4 | `ifmo-automata/vashkevich-nda.pdf`, `_application-pdf.pdf`, `web/wiki-en-powerset-construction.pdf`, `wiki-en-dfa-minimization.pdf`, `ifmo-works/akishev-dvorkin-2010-full.pdf` |
| 5 | `web/wiki-en-thompson-construction.pdf`, `wiki-en-kleene-algorithm.pdf`, `wiki-en-pumping-lemma.pdf`, `wiki-en-regular-expression.pdf` |
| 6 | `web/Cox-regular-expression-matching-can-be-simple-and-fast.pdf`, `wiki-en-redos.pdf`, `wiki-en-ragel.pdf`, `ifmo-works/11-` и `12-Paraschenko-Stankevich.pdf` |
| 7 (АП) | `ifmo-books/_book.pdf` гл. 2–3, `_umk.pdf`, `ifmo-science/MS_Tests_CLR.pdf`, `ifmo-disser/stepanov_disser.pdf`, `shamg_disser.pdf`, `ifmo-progeny/kuznetsov-shalyto.pdf`, `ifmo-automata/_autmicroc.pdf`, `_autmicroc2.pdf`, `web/wiki-en-state-pattern.pdf` |
| 8 (statecharts) | `web/wiki-en-uml-state-machine.pdf`, `statecharts-dev-what-is-a-statechart.pdf`, `wiki-en-sequential-function-chart.pdf`, `wiki-en-iec-61131-3.pdf`, `wiki-en-scxml.pdf`, `ifmo-disser/gurov_disser.pdf`, `external/1-s2.0-0167642387900359-main.pdf` |
| 9 (верификация) | `ifmo-verification/velder_verification_posobie.pdf`, `_LTL_for_Spin.pdf`, `alarm_clock_verification.pdf`, `ifmo-books/_principles_of_model_checking.pdf`, `_model-based_testing_of_reactive_systems.pdf` |
| 10 (МТ и КА) | `web/wiki-en-chomsky-hierarchy.pdf`, `wiki-en-turing-machine.pdf`, `wiki-en-halting-problem.pdf`, `wiki-en-busy-beaver.pdf`, `wiki-en-rule-110.pdf`, `wiki-en-langtons-ant.pdf`, `ifmo-papers/_ln_Thesis.pdf`, `ifmo-disser/ulyantsev-dissertation.pdf` |

Нумерация в этой таблице соответствует плану `REPORT.md` § 6, где машина
Тьюринга — лекция 7, а автоматное программирование — лекция 9. В нашей
нумерации автоматное программирование — лекция 7, statecharts — 8,
верификация — 9, машина Тьюринга — 10. При сверке с `DOCS.md` держать это
в уме.

### Чего в корпусе нет (нужно достать легально)

`DOCS.md` часть III перечисляет источники, найденные, но не загруженные:

- Harel D. Statecharts (1987) — **есть** в `external/`;
- Шалыто А. А. SWITCH-технология (Наука, 1998, 628 с.);
- Samek M. Practical UML Statecharts in C/C++, 2-е изд. (2008);
- **Хопкрофт — Мотвани — Ульман** «Введение в теорию автоматов, языков и
  вычислений» — закрывает все теоретические лакуны § 3.2 REPORT.md;
- **Кудрявцев — Гасанов — Подколзин** «Теория интеллектуальных систем,
  кн. 4» — источник, из которого целиком взят раздел «Регулярные события»;
  нужен, чтобы восстановить леммы № 1–5;
- Cook M. Universality in Elementary Cellular Automata (2004) — для правила 110.
