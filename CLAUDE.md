# statecraft — контекст репозитория

Учебный курс «Автоматное программирование систем управления» (АПСУ): лекции,
практикум на C90, модели SimInTech, примеры Promela/SPIN. Origin — <https://github.com/BasePractice/statecraft>.
Исторически репозиторий назывался `automata_programming` (следы остались в
абсолютных путях внутри сгенерированного кода).

## Где что лежит

| Путь | Что это |
|---|---|
| `CMakeLists.txt` | Корень сборки: одной командой собираются и PDF лекций, и примеры |
| `cmake/StatecraftStrictC.cmake` | Общий режим компиляции: ISO C90, `-Werror` |
| `scripts/check-style.sh`, `.clang-format` | Оформление кода: формат и его проверка |
| `scripts/check-promela.sh` | Разбор и верификация моделей Promela (SPIN внешний, при отсутствии — мягкий пропуск) |
| `.github/workflows/ci.yml` | CI: оформление, практикум (ubuntu + macOS × Debug + Release), лекции с выкладкой PDF |
| `lectures/` | **Основное**: 12 лекций на typst под единым шаблоном. См. `lectures/README.md` |
| `practices/` | Практикум на C90: каталоги названы `<номер лекции>-<тема>` в kebab-case, см. `practices/README.md` |
| `practices/common/` | Общий код практикума: `base_types.h`, точка входа тестов, `network/`, заголовок Catch2 |
| `practices/resources/` | Сторонние ресурсы (сборки SPIN под Windows) |
| `docs/` | Отчёты и служебные данные: разбор MISRA (`misra-report.md`, `misra-rules.txt`) |
| `video/` | Скринкасты курса: настройка кодогенератора SimInTech |
| `Description.md` | Фактический README курса: цели, требования, порядок приёма лабораторных |
| `REPORT.md` | Отчёт проверки от 16.08.2026: сборка, дефекты кода и лекций, соображения по развитию курса |
| `PLAN.md` | План работ: что дописать в каждой лекции, очерёдность, карта источников |
| `КР_15.11.2019.txt` | 30+ тем курсовых работ — источник прикладных примеров |

## lectures/ — как устроено

Полное описание в `lectures/README.md`. Коротко:

- `course.typ` — **единственное** место правки автора, института, кафедры,
  даты, версии и реестра лекций. Этот же файл читают bash-скрипты
  через `typst eval`, поэтому список лекций нигде не дублируется.
- `template/` — шаблон: `theme`, `i18n`, `blocks`, `code`, `figures`,
  `frontmatter`, `appendix`, `lecture`. Базовый шаблон не зависит ни от
  одного `@preview`-пакета и собирается офлайн.
- `shared/` — единая нотация, глоссарий, банк контрольных вопросов и задачник.
  Вопрос в банке — пара (текст, метка раздела), отсылка «§ 4.2» печатается
  ссылкой и номер берёт счётчиком заголовков. Отсюда правило: **новый вопрос
  заводится вместе с меткой** раздела, где на него есть ответ; ссылка на
  несуществующую метку роняет сборку лекции, и это единственная имеющаяся
  проверка того, что банк не разошёлся с лекциями.
- `src/a<N>-<id>/` — **приложения курса**: не лекции, номера в расписании у
  них нет. Реестр — `appendices` в том же `course.typ`, печатаются тем же
  шаблоном с параметром `appendix-id` (на титуле и в колонтитуле вместо
  «Лекция N» стоит «Приложение»), собираются и пакуются вместе с лекциями.
  Сейчас их два: `a1-version-control` (Git, ТД-6) и `a2-code-style`
  («Требования к коду курса», 18.08.2026) — единый документ о режиме сборки,
  оформлении, комментариях, тестах и порядке самопроверки перед сдачей.
- `src/lab-N-<тема>/` — **лабораторные работы**: задание, варианты, порядок
  сдачи и критерии приёмки. Реестр — `labs` в том же `course.typ` (третий
  рядом с лекциями и приложениями), в шаблоне за них отвечает параметр
  `lab-id`. Собираются и пакуются вместе с лекциями, а с 18.08.2026 входят и
  в сводный том — приложениями после справочных (В–Ж), чтобы том был полным
  комплектом курса. Написаны
  пять: `lab-1-synthesis` (лекция 3), `lab-2-dfa` (4), `lab-3-format` (6),
  `lab-4-turing` (10), `lab-5-three-ways` (7). У каждой 22–30 вариантов, и
  каждый шаг задания имеет машинную проверку — прогон практики, тест или
  сверку с порождённым эталоном.
- `book.typ` + `template/book.typ` — **сводный том**: все лекции и приложения
  одним PDF (337 страниц на 18.08.2026) с единым оглавлением, сквозной
  нумерацией и одним списком литературы. Текст берётся из тех же
  `main.body.typ`, поэтому разойтись с отдельными лекциями он не может; в `book.typ` руками перечислены
  только пути включений — `include` в typst принимает лишь литеральный путь.
  Собирается вместе с комплектом (`make`, `make pack`) или отдельно
  (`make book`). Лекция в томе — раздел первого уровня, её разделы уезжают на
  уровень ниже через `set heading(offset: 1)`.
- `src/<id>/main.typ` + `main.body.typ` — лекция. Разделение нужно, чтобы
  `scripts/tex2typ.sh` можно было перезапускать, не теряя ручной обвязки.
- `scripts/` — `check.sh`, `build.sh`, `clean.sh`, `sync-code.sh`,
  `fetch-fonts.sh`, `vendor-packages.sh`, `tex2typ.sh`.
- Сборка лекций описана **только** в `lectures/Makefile` и `scripts/`:
  `lectures/CMakeLists.txt` вызывает `make pretty OUT=<build>/lectures`, а не
  повторяет логику компиляции. Поэтому лекции собираются и без CMake
  (`cd lectures && make`), и порядок сборки в обоих случаях один.
- `scripts/sync-code.sh` держит листинги лекций (`src/<id>/code/`) равными
  файлам практикума; соответствие — в `scripts/code-map.txt`, проверка
  `make sync-check` стоит в CI. Копии нужны потому, что typst читает файлы
  только из своего `--root`.
- `syntaxes/*.sublime-syntax` — подсветка языков, которых typst не знает:
  Takt, Promela и БНФ. Подключаются в `template/code.typ`
  (`set raw(syntaxes: ...)`); без них листинг набирается одним цветом, то есть
  неотличимо от вывода программы. Язык обязателен у **каждого** листинга, в
  том числе у вставок `#raw("...", block: true)` — их оставила конвертация из
  LaTeX, и именно там подсветка терялась незаметно. Проверка — `make
  highlight`: она читает собранные PDF и считает цвета текста на плашках
  листингов, а не смотрит в исходники.
- `template/diagrams.typ` — единственный файл с `@preview`-зависимостями
  (fletcher для диаграмм состояний, CeTZ для структурных схем). Подключается
  только теми лекциями, где есть векторные рисунки; кэш прогревается
  `scripts/vendor-packages.sh`. Карты Карно (`karnaugh-map`) внешних пакетов
  не требуют и живут в `template/figures.typ`.

```bash
cmake -S . -B build && cmake --build build -j   # весь курс: PDF + примеры
ctest --test-dir build                          # тесты примеров
./scripts/check-style.sh                        # оформление кода

cd lectures && make fix && make                  # только лекции (make = make pretty)
cd lectures && make sync-check                  # листинги лекций против practices/
```

### Ловушки typst 0.15, на которые уже наступили

- Угловые скобки — только `chevron.l` / `chevron.r`. `angle.l` даёт
  «unknown symbol modifier».
- Единственный встроенный ГОСТ-стиль библиографии — `gost-r-705-2008-numeric`
  (не `gost-r-7-0-5-2008`).
- Fira Code установлена как variable-шрифт с Default weight 300 (Light) —
  вес задан явно в `template/theme.typ`.
- Счётчик `figure` шагает по первому уровню и «схлопывает» двумерное
  значение, поэтому утверждения нумеруются сквозным номером по лекции, а не
  «раздел.номер».
- `read()` и `image()` внутри файлов шаблона разрешают относительные пути от
  самого шаблона: в `code-file` и `img` нужен абсолютный путь от корня
  (`/src/<id>/code/foo.c`).
- Основной текст лекций набирается **Fira Code**: переключатель
  `body-in-mono` в `template/theme.typ` установлен в `true` (требование
  заказчика курса). В этом режиме кегль берётся из `mono-body-size` (9.6 pt
  вместо 11 pt), интерлиньяж — из `mono-body-leading`. При
  `body-in-mono: false` основной текст — PT Serif с запасным Libertinus Serif.
- **Выключка по формату включена всегда** (18.08.2026, требование заказчика:
  правый край должен быть ровным). Раньше при `body-in-mono` она была
  отключена — моноширинный шрифт не тянется по ширине знака, и вся растяжка
  приходится на пробелы. Против дыр работают три вещи, все в
  `template/theme.typ`: `hyphenate: true`, удешевлённый перенос
  (`hyphenation-cost`, 35 % вместо 100 % — typst охотнее переносит слово, чем
  растягивает строку) и отключённая выключка там, где строка короткая, —
  внутри таблиц (`justify-in-tables`) и в подписях (`justify-in-captions`).
  Прогон по собранным PDF: строк с пробелом шире полутора нормальных — единицы
  на лекцию, и почти все они — строки с формулами, где `pdftotext` не видит
  математику.
- **`figure` не разрывается между страницами, а `raw` внутри него — да.**
  Листинг длиннее страницы просто вылезал за нижнее поле, и подпись с
  колонцифрой печатались **поверх кода** (девять страниц тома, лекции 6–10).
  Лечится `show figure.where(kind: raw): set block(breakable: true)` в
  `template/figures.typ`; то же сделано для таблиц.
- **Номер строки листинга и код набираются двумя колонками (`grid`).** Пока
  номер был `box`-ом перед телом строки, перенос длинной строки уходил под
  номера и читался как новая строка кода. Висячий отступ (`par(hanging-indent)`)
  внутри `raw` не работает — проверено, поэтому колонки.
- **Отступы заголовков вынесены в `heading-gaps`** (`template/theme.typ`):
  пары «above/below» для части тома, раздела, подраздела, пункта и служебного
  раздела. Одни и те же значения применяют и `template/lecture.typ`, и
  `template/book.typ` — уровни в них сдвинуты на один, поэтому ключи названы
  по смыслу, а не по номеру уровня. Служебные разделы («Содержание», списки
  рисунков, таблиц и листингов, «Обозначения и сокращения», «Контрольные
  вопросы», «Задачи», «Список литературы») отбираются по признаку «заголовок
  без номера» и центрируются (`service-heading-align`).
- **Слабая отбивка вытесняет блочную, а не берёт с ней максимум.** Строка
  списка первого уровня начинается с `v(6pt, weak: true)`, и из-за этого
  `below` у заголовка списка пропадал: сколько ни увеличивай отступ, первая
  строка липла к заголовку. Лечится явной (не `weak`) отбивкой после
  заголовка — так и сделано в `front-outlines`. Там же заголовок печатается
  отдельно, а не параметром `title` у `outline`.
- **Заголовок с `above: 0pt` сразу после `outline` накладывается на последнюю
  строку списка** — не «прилипает», а рисуется поверх. Поэтому у служебных
  заголовков `above` ненулевой; в начале страницы этот отступ всё равно
  отбрасывается.
- **В osifont три кириллических глифа нарисованы залитым прямоугольником:
  «ё», «Ё» и прописная «Э».** Запасной шрифт не помогает: typst подставляет
  его только когда глифа нет вовсе, а здесь он формально есть.
  `template/diagrams.typ` подменяет все три основным шрифтом (`_glyph-fix`);
  при добавлении новых надписей на рисунках это уже работает само. Список
  проверен 01.09.2026 сплошным рендерингом алфавита — **и только так его и
  можно проверить**: разбор `glyf` обманывает, «Ё» набрана прямоугольником с
  точками сверху и по числу контуров неотличима от нормальной буквы. Строчная
  «э», в отличие от прописной, нарисована.
- **Метки заголовков в томе обязаны быть уникальны по всему курсу.** Сборник
  включает все лекции в один документ, и одинаковые `<sec:...>` в разных
  лекциях сталкиваются. Проверять при заведении новой метки; сборка `book.typ`
  падает на конфликте.
- `fsm-diagram` вписывает диаграмму в полосу набора, если она шире (цепочка
  из шести состояний при `spacing: 3.2cm` уезжала за поля). У состояния есть
  `initial` / `final` (двойной контур) и `entry` (входящая стрелка).

## Откуда взялись лекции

Материал сведён из трёх источников:

1. `/Users/pastor/github/articles` — четыре лекции на LaTeX
   (classicthesis): «Конечные автоматы», «Машина Тьюринга», «Регулярные
   события», «Автоматное программирование», плюс методичка курсовой работы
   на 188 вариантов. Курсовая в `lectures/` **не** перенесена.
2. Md-лекции этого репозитория (каталоги `00.History` … `08.Promela_Spin`,
   удалены 16.08.2026 после переноса содержимого).
3. `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ` — `REPORT.md` (856 строк,
   разбор содержательных ошибок и план курса из 10 лекций),
   `SOURCE-REPORT.md` (288 строк, разбор LaTeX-исходников с точностью до
   строки), `docs/` (402 источника, `bibliography.bib` на 406 записей).

**Нумерация лекций провизорная**: она сведена из трёх источников и
подлежит согласованию. Перенумерация — правка реестра в `course.typ` плюс
`git mv` каталога в `src/`.

## Что уже исправлено при переносе

Ломавшее вёрстку (материал физически отсутствовал в выдаваемых PDF):

- `multiline` вместо `multline` — не выводились **пять** итоговых булевых
  формул синтеза схемы разменного аппарата;
- `\end{figure}` до `\caption` — разваливался рисунок «Анализ автомата
  акцептора»;
- два `\begin{cases}` в одном `equation` — не выводились кодировки выходных
  символов и состояний;
- битая команда `\dtos`, неэкранированный `&`.

Содержательное (коды из `REPORT.md`):

| Код | Что исправлено |
|---|---|
| К-1 | Удалено «при помощи КА можно описать любой алгоритм»; написан раздел о том, что добавляет лента, с иерархией Хомского |
| К-2 | Снежинка Коха больше не подаётся как пример невычислимости — вместо неё busy beaver и колмогоровская сложность |
| К-3 | Разведены факторизация (RSA) и дискретный логарифм (Диффи — Хеллман) |
| К-4 | Канонические уравнения разменного аппарата: `mod 3` и `⌊⌋` |
| К-5 | Определение регулярного выражения (повторяло определение события) |
| К-6 | `M⁺` — итерация, а не произведение |
| С-1 | Таблица машины Тьюринга для `y = x+1` дополнена столбцом `q₂` |
| С-2 | Индекс состояния в определении команды МТ; «в состояние q₁» |
| С-4 | Тезис Чёрча — Тьюринга отделён от физического |
| С-7 | Вставлены листинги 1 и 2 (ссылки вели в никуда) |
| С-8 | Добавлена формулировка теоремы Клини |
| С-10 | Разделены определения функций переходов и выходов в лемме № 8 |
| С-11 | Исправлен пример ревнивой квантификации (`a*a` против `a*+a`) |
| С-12 | «Мили», а не «Миля» |
| С-13 | «B — алфавит выходных символов» |
| — | Формула $\varphi_2$ синтеза разменного аппарата: четвёртая конъюнкция и свёрнутая форма не совпадали с таблицей; исправлено минимизацией — и в отдельной формуле, и в итоговой системе канонических уравнений |
| — | 11 меток рисунков после конвертации оказались привязаны к заголовкам вместо самих рисунков |

Плюс: раскомментированы листинги реализации МТ на Си; единая нотация
(`q₀` — начальное, `F` — заключительные, `ε` — пустое слово); слиты четыре
`bibliography.bib` в один с починкой записей `MT_And_Recursion_Functions`
(состояла из знаков вопроса) и `Kuz_5` (Harel, была оформлена как `@book`).

## Проверка 16.08.2026 — что она дала

Полный протокол — в [REPORT.md](REPORT.md). Здесь то, что важно помнить при
следующих правках.

**Release ловит то, чего не видит Debug.** `char_list_find` в машине Тьюринга
не возвращала значения при неудачном поиске: путь заканчивался `assert(0)`,
который в Release исчезает вместе с `NDEBUG`. Debug собирался чисто, Release
падал с `-Wreturn-type`. Поэтому в матрицу CI добавлен Release — без него
такие дефекты не видны. Правило: `assert` не заменяет `return`.

Это повторилось на новом коде в тот же день: в `03-synthesis` импликанты
заполнялись частично, разряды сверх `var_count` оставались мусором со стека.
Debug и санитайзеры молчали (мусор оказывался нулевым), Release уронил два
теста. Правило: структуру с массивами фиксированного размера обнулять
целиком, даже если «используется только начало».

**Практика без тестов — практика без гарантий.** 18.08.2026 выяснилось, что
`03-control-program` не проверялся ничем: тестов не было, а эмулятор печатал
протокол, который никто не сверял. Написанная для теста модель установки нашла
дефект первым прогоном: смещения точек сварки задаются со знаком (`+3` —
вправо, `−3` — влево), а сравнивались со счётчиком шагов, который считает
вверх, поэтому точка левее начала поиска не находилась никогда — аппарат уезжал
влево, пока не снимут питание. На первой точке (`+3`) автомат работал верно, и
дефект был не виден. Правило: каталог практикума без теста в `ctest` —
это не «простой пример», а непроверенный код.

**Заглушка вместо точки входа — тоже непроверенный код.** 18.08.2026 у
лексера (`06-lexical-analyze`) появился CLI, печатающий поток лексем, и первый
же прогон показал два дефекта, прожившие в коде всё время: у односимвольных
лексем (`+`, `*`, скобки) не сдвигался `it_end`, поэтому длина лексемы
выходила нулевой, а в разборе стояла недостижимая ветвь — условие «закрывающая
скобка» было записано дважды, и во второй раз скобке приписывался вид
`TokenMul`. Тесты этого не видели, потому что проверяли вид лексемы, а текст
брали только у чисел и имён. Правило: если на работу модуля нельзя посмотреть,
её никто и не смотрит.

Там же нашлось расхождение лекции с практикой: § «Пример: язык арифметических
выражений» лекции 6 приводил грамматику с `&`, `|` и `ROLE`, приписывая её
практикуму, который разбирает арифметику. Грамматика заменена настоящей.

**Зелёные тесты ничего не гарантируют.** В том же файле были выход за границы
ленты, утечки всей таблицы переходов и выделение памяти в восемь раз больше
нужного — при 100 % проходящих тестах. Тесты подавали только корректные
данные. Санитайзеры (`-fsanitize=address,undefined`) тоже молчали по той же
причине.

**Кириллица в формулах.** После конвертации из LaTeX в 29 формулах стояли
русские `А`, `В`, `С`, `М` вместо латинских — визуально неотличимо. При
правке перенесённых лекций стоит прогонять проверку: короткие формулы
`$...$`, содержащие символы из диапазона `[А-Яа-я]`, почти всегда ошибка.

**Запятая внутри `cases()` — разделитель аргументов.** Условие вида
`y(t) = x(t-1), t = 2, 3, …` typst разбирает как четыре строки системы.
Лечится скобками: `y(t) = x(t-1) quad (t = 2, 3, dots)`.

**Модели Promela заявляют ожидаемый исход сами.** Строка
`verify: ok|violation|skip` в шапке модели: часть примеров курса
демонстрирует именно нарушение, и для них отсутствие нарушения — тоже
ошибка. Проверка: `./scripts/check-promela.sh --verify`.

**Копипаста типов между проектами практикума.** `02-fsm-delay` объявлял
`InsertingEngine` — тип из `07-fsm-insert-element` с другим содержимым.
При заведении нового проекта имена типов проверять по всему `practices/`.

## Что осталось сделать

**Детальный план работ — в [PLAN.md](PLAN.md).** Там разбор по каждой из
десяти лекций: что есть, что дописать, из какого источника корпуса АПСУ
брать, чем проверять; сквозные работы (глоссарий, задачник, лабораторные),
очерёдность и список граблей typst. Ниже — только сводка.

Ненаписанное помечалось в PDF красными блоками «НЕ НАПИСАНО» (`#todo[...]`);
на 18.08.2026 в лекциях таких блоков **не осталось вовсе** — последний живёт
в болванке `src/_template`. Проверка:

```bash
grep -rn '#todo\[' lectures/src/
```

Флаг `show-todo: false` в `course.typ` убирает такие блоки из PDF перед
раздачей студентам.

Крупное из оставшегося (подробно — в `PLAN.md`):

- `implementation_gl.tex` из `articles` (647 строк, реализация «Жизни» и
  «умного муравья» на C++) не перенесён — в исходнике вклеивался готовым
  PDF через `\includepdf`. Смысла переносить сейчас немного: и «Жизнь», и
  муравей есть в практикуме своим кодом на C90 (`11-cells`, `11-ant-search`),
  причём с исправленными дефектами оригинала.

## Технический долг

Задачи, поставленные заказчиком курса и не привязанные к одной лекции. Каждая
описана подробнее в [PLAN.md](PLAN.md).

### ТД-1. ГОСТ-шрифт в диаграммах — закрыт 18.08.2026

Все надписи внутри рисунков — подписи состояний, метки дуг, тексты на
структурных схемах и statechart'ах — набираются **чертёжным шрифтом по
ГОСТ 2.304-81** (ISO 3098). Сделано:

- `fonts.diagram` в `template/theme.typ` (сейчас — `osifont`, свободная
  реализация ISO 3098 под LGPL3 + font exception);
- `template/diagrams.typ` навешивает его на содержимое `figure` у
  `fsm-diagram` и `scheme`. Важно: именно на содержимое, а не `set text` на
  выходе функции — иначе метка `<fig:…>` цепляется к styled-контенту и
  ссылки на рисунок ломаются («cannot reference styled»);
- шрифты лежат **в репозитории** (`lectures/fonts/`, 5 МБ, вместе с текстами
  лицензий и `README.md`): сборка не зависит ни от установленного в системе,
  ни от сети. `scripts/fetch-fonts.sh` (`make fonts`) остаётся рабочим и
  идемпотентным — качает те же файлы по конкретным коммитам с проверкой
  sha256; `scripts/check.sh` (`make check`) проверяет наличие. Отсутствие
  шрифта сборку не роняет — надписи набираются основным шрифтом лекции, и
  именно поэтому его отсутствие раньше терялось: в потоке сборки оно видно
  только предупреждением typst «unknown font family: osifont».

**Закрыт 18.08.2026.** Оставшиеся два вопроса решены:

- формульные метки (`$q_0$`, `$1\/0$`) набираются математическим шрифтом
  курса — **так и остаётся**, решение согласовано с заказчиком 18.08.2026.
  Довод: формула в подписи состояния и та же формула в тексте лекции обязаны
  выглядеть одинаково, иначе диаграмма читается как чужая вставка. ГОСТ-шрифт
  остаётся на том, что является надписью, а не формулой: имена сигналов,
  тексты на структурных схемах, подписи блоков;
- растровых диаграмм в лекциях не осталось вовсе (ТД-7, 17.08.2026), поэтому
  вопрос «что делать с ними» отпал сам.

### ТД-2. Сверка лекций с первоисточниками

Провести повторный сплошной анализ всех лекций: сопоставить написанный текст
с корпусом источников и подкрепить **основные тезисы, утверждения, суждения и
формулы** ссылками на первоисточник.

- корпус: `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ/docs/` (402 файла) и
  каталог с аннотациями `~/Yandex.Disk.localized/Учеба/Лекции/АПСУ/DOCS.md`;
- карта «какой источник к какой лекции» — `PLAN.md` § 11 и `DOCS.md`, часть IV;
- каждую новую ссылку заводить записью в `lectures/bib/references.bib` и
  ссылаться штатным `@key`, а не текстом в скобках;
- особое внимание — местам, перенесённым из старых конспектов без указания
  происхождения, и формулам, выведенным при переносе заново;
- результат каждой лекции фиксировать во врезке `#sources[...]`.

#### Порядок работы (сложился 17.08.2026)

Врезка `#sources[...]` изначально была журналом переноса — «что исправлено,
что дописано». Теперь у неё две части: журнал остаётся, а к нему добавляется
перечень первоисточников с ключами, которыми подкреплены основные тезисы
лекции. Так видно и происхождение материала, и то, что именно сверено.

Готовые записи BibTeX для источников корпуса берутся из `DOCS.md` — там у
каждого артефакта есть библиографическое описание и блок `bibtex`. Для книг и
стандартов, которых в корпусе нет, запись составляется по самому документу
(ISO, IEC, RFC, OMG).

#### Прогресс

Первый проход сделан по всем двенадцати лекциям (17.08.2026): у каждой есть
список литературы, ключевые тезисы подкреплены первоисточниками, врезка
`#sources` перечисляет, чем именно.

| Лекция | Источников в PDF | Чем подкреплены основные тезисы |
|---|---|---|
| 1 | 23 | Хопкрофт, Сипсер (определения); Марков, Успенский—Семёнов (алгоритм); Дейкстра, Дал—Дейкстра—Хоор, Вирт, Мейер, Ковальски, Кэй, фон Нейман (парадигмы); Шалыто, Анисимов, Дединский (автоматное программирование); Ритчи, `K&R`, три редакции ISO (язык Си) |
| 2 | 8 | Мили, Мур, Мак-Каллок—Питтс, Хаффман |
| 3 | 5 | Шеннон (булева алгебра схем), Хаффман, Карно, Квайн, Мак-Класки |
| 4 | 5 | Рабин—Скотт (НКА и подмножества), Нероуд (критерий регулярности), Мур (разбиение, длина различающего слова), Хопкрофт (минимизация за $n log n$) |
| 5 | 6 | Клини (теорема), Мак-Каллок—Питтс (сети, для которых она доказана), Бар-Хиллел—Перлес—Шамир (накачка), Томпсон |
| 6 | 9 | Томпсон, POSIX, Perl, Кокс, постмортем Cloudflare 2019, «книга дракона» |
| 7 | 19 | Кузнецов (текст раздела и его ссылки), Шалыто и Туккель (SWITCH-технология), Харел, GoF, Чоу |
| 8 | 12 | Харел (нотация и семантика STATEMATE), UML 2.5.1, IEC 61131-3, IEC 60848, SCXML, Самек, RFC 793 и 9293 |
| 9 | 9 | Пнуэли (LTL), Кларк—Эмерсон (проверка моделей), Варди—Волпер (автоматный подход), Хольцман (SPIN), Вельдер |
| 10 | 38 | Чёрч, Тьюринг, Пост (тезис), Хомский (иерархия), Кук и Карп (NP-полнота) |
| 11 | 11 | Гарднер (первая публикация «Жизни»), фон Нейман, Лэнгтон, Кук (правило 110), Вольфрам |
| 12 | 4 | документация языка Takt, IEC 61131-3, Пнуэли, Кларк—Эмерсон |

#### Второй проход: сверка выкладок вычислением (17.08.2026)

Первый проход отвечал на вопрос «откуда взят тезис», второй — «сходятся ли
цифры». Проверялось не чтением, а прогоном: то, что в лекции набрано руками,
сравнивалось с тем, что печатает практикум или даёт независимый счёт.

| Что сверено | Как | Итог |
|---|---|---|
| лекция 2: подписи дуг на диаграммах Мура (задержка, сумматор, сумма по модулю 2) | сопоставление каждой дуги с системой канонических уравнений скриптом | сходится |
| лекция 3: пять карт Карно | порождены из `machine/coin.fsm` и сравнены с набранными | сходится |
| лекция 3: формулы `φ₁, φ₂, ψ₁, ψ₂, ψ₃` | прогон `03-synthesis` (Квайн — Мак-Класки) | те же конъюнкции, свёрнутые формы проверены раскрытием |
| лекция 4: ε-НКА, ε-замыкания, таблица ДКА, минимальный автомат | `05-kleene "(a\|b)*abb" \| 04-dfa-nfa --minimize` | сходится: 16 переходов, 5 подмножеств, 4 состояния |
| лекция 5: правило «два состояния на символ выражения» | прогон на пяти выражениях | верно, если считать символами и `\|`, и `*`; уточнено в тексте |
| лекция 10: машины `M₁`, `M₂`, `M₃` | симуляция по таблицам | поведение совпадает с описанием, `M₃` не останавливается |
| лекция 10: таблица `y = x + 1` | прогон на всех `x < 2000` | функция вычисляется верно |

**Найдено:** в таблице `y = x + 1` столбцы `q₁` и `q₂` совпадают — второе
состояние для этой функции избыточно, машина считает и с одним (проверено).
Таблицу не переписывали: разделение «первый разряд» и «перенос» осмысленно как
изложение алгоритма. Вместо этого добавлено замечание, что столбцы одинаковы,
и упражнение с `y = x − 1`, где они уже различаются.

Что осталось: леммы 1–8 в лекции 5 сверить с книгой @TIS построчно нельзя —
её нет ни в корпусе `docs/`, ни в открытом доступе (`DOCS.md`, часть III,
перечисляет её среди ненайденных). Проверена внутренняя согласованность:
нумерация лемм, ссылки на них и построение автомата по лемме 7 совпадают с
практикой `05-kleene`.

Раздел о парадигмах в лекции 1 — характерный случай, ради которого задача и
заведена: он был сведён из энциклопедических статей, содержал дословную цитату
Б. Мейера без указания источника и утверждения об истории без ссылок.

### ТД-3. CI/CD и публикация версий курса — закрыт 17.08.2026

Проверить конвейер GitHub Actions целиком (сейчас он ни разу не прогонялся на
всех заданиях одновременно) и добавить выпуск релиза.

- прогнать и починить существующие задания `.github/workflows/ci.yml`:
  оформление кода, практикум (ubuntu + macOS × Debug + Release), модели
  Promela, лекции;
- добавить факультативное задание для графики: сборка `21-additional-gui`
  (`-DSTATECRAFT_BUILD_GUI=ON`) на ubuntu и macOS с установленной raylib.
  Запустить окно в CI негде, поэтому задание проверяет компиляцию и то, что
  без дисплея приложение завершается с сообщением, а не падает. Оттуда же
  берётся проверка переносимости, оставшаяся от ТД-9 (сейчас графика собрана и
  проверена только на macOS; HiDPI и малые экраны не проверялись);
- отдельное задание на публикацию: срабатывает при постановке тега вида
  `v{MAJOR}.{MINOR}.{BUILD}`, собирает PDF (`make pretty`) и выкладывает их
  как артефакты релиза GitHub;
- релиз оформляется при изменении **минорной** версии; сборочный номер
  (`BUILD`) артефактов не публикует;
- версия в теге должна совпадать с `version` из `lectures/course.typ` —
  проверять это в задании и падать при расхождении. С 17.08.2026 версия курса
  ведётся в том же формате, что и теги: `1.0.31` вместо прежнего `2026.1`
  (первый релиз, на который поставлен тег). Номер печатается на титульном
  листе каждой лекции, поэтому менять его — значит переиздавать весь комплект;
- имена файлов релиза — «читаемые» (`03 — Синтез автоматов и автоматные
  схемы.pdf`), их уже делает `make pretty`.

#### Что сделано 17.08.2026

**Конвейер не был зелёным ни разу.** Из четырёх заданий падали три, и каждое
падение указывало на настоящий дефект, а не на каприз CI.

| Где | Что было |
|---|---|
| практикум, ubuntu | Catch2 v2.3.0 считает размер альтернативного стека через `MINSIGSTKSZ`, а с glibc 2.34 это вызов `sysconf()` — выражение перестало быть `constexpr` |
| практикум, ubuntu | `usleep` в `03-control-program` не объявлен: это POSIX, а не ISO C90, и в строгом режиме glibc прячет объявление за макросом тестируемых возможностей |
| практикум, ubuntu | `11-cells/render.c`: буфер номера состояния на 8 байт при `sprintf("%d")` до 11 знаков (`-Wformat-overflow`) |
| практикум, ubuntu | `06-lexical-analyze`: результат `fread` не проверялся вовсе, а чтение шло одной записью длиной в файл — в текстовом режиме такой вызов возвращает ноль при успешно прочитанном файле |
| практикум, ubuntu | `cos`, `sin`, `sqrt` из `render.c` не линковались: на Linux математика в отдельной `libm` |
| практикум, ubuntu, Release | `03-synthesis`: `row_to_bits` заполняла массив по `var_count`, оставляя разряды сверх него мусором со стека; `21-additional`: `parse_int` и `read_int` не задавали значение на пути ошибки |
| оформление кода | на macOS у `grep` нет ключа `-P`, а ошибка глушилась: проверки табуляции и `//` молча ничего не находили — **две проверки из семи на машине разработчика были пустышкой** |
| оформление кода | разбор `//` регулярным выражением ложно срабатывал на слове «комментарии //» внутри блочного комментария |
| оформление кода | clang-format 18 и старше сжимает завершающие комментарии, с 19-й они выравниваются в столбик: CI объявлял расходящимися 12 файлов, оформленных правильно |
| лекции | GitHub не принимает артефакт с двоеточием в имени файла — его давала лекция 12 («Язык Takt: описание…»); на NTFS такой файл и вовсе не сохранить |
| модели Takt | модели `12-takt` объявляли `type u8 = [bit;8]`, а в текущей версии Takt `u8` — встроенное имя, переобъявлять нельзя (SE-107). Не было видно, потому что `taktc` в CI не запускался, а локальный компилятор старее |

**GCC стоит держать локально.** Половина дефектов выше — расхождение clang и
GCC, и все они ловятся до отправки: `brew install gcc` даёт `gcc-16`, а
`cmake -DCMAKE_C_COMPILER=gcc-16 -DCMAKE_CXX_COMPILER=g++-16
-DCMAKE_BUILD_TYPE=Release` воспроизводит сборку CI на ubuntu. Правило то же,
что с Release из проверки 16.08.2026: чем раньше меняется компилятор, тем
дешевле дефект.

**Задания конвейера** (`.github/workflows/ci.yml`):

- `style` — оформление кода; clang-format ставится из PyPI зафиксированной
  версией, поэтому одинаков на любой системе. Ключ `--require-clang-format`
  делает отсутствие подходящей версии ошибкой, а не тихим пропуском;
- `practice` — практикум, ubuntu + macOS × Debug + Release;
- `promela` — разбор и верификация моделей;
- `lectures` — окружение, `sync-check`, сборка PDF, счётчик `#todo`, выкладка;
- `takt` — сборка `taktc` и `takt-sim` из исходников (ubuntu + macOS,
  с кешем cargo, ~2,5 мин), инструменты выкладываются артефактом;
- `gui` — сборка `21-additional-gui` с raylib, проверка, что цели собраны, а
  не пропущены, прогон тестов (с `taktc` проверяются и `12-takt`, и
  `21-additional`) и требование ТД-9: без дисплея приложение обязано
  завершиться сообщением, а не падением.

Два последних задания помечены `continue-on-error`: они зависят от внешних
инструментов, и их отказ не должен закрывать дорогу материалу курса. Пакета
raylib в Ubuntu 24.04 нет вовсе, а бинарный архив с сайта проекта идёт без
cmake-конфигурации, поэтому библиотека собирается из исходников — версии 6.0,
той же, что ставит `brew`.

**Публикация** (`.github/workflows/release.yml`): срабатывает на тег
`v{MAJOR}.{MINOR}.{BUILD}`, сверяет его с `version` из `course.typ`
(`lectures/scripts/course.sh --check`), собирает комплект (`make pack`) и
оформляет релиз **только при нулевом сборочном номере** — теги вида `v1.0.31`
отмечают сборку, PDF выкладываются артефактом прогона, но список релизов
промежуточными версиями не заполняется. Комплект пакуется python, а не `zip`:
Info-ZIP помечает русские имена кодировкой CP437, и «Проводник» Windows
показывает вместо названий мусор.

**Конвейер зелёный целиком** (прогон 32050... от 17.08.2026): все одиннадцать
заданий проходят. Требование ТД-9 подтверждено в настоящей среде без дисплея —
на ubuntu приложение печатает «окно не создано: графический вывод недоступен»
и возвращает 1, а не падает по сигналу.

**Первый публикуемый выпуск — 1.1.0**, тег `v1.1.0` от 17.08.2026: релиз
[оформлен](https://github.com/BasePractice/statecraft/releases/tag/v1.1.0)
самим конвейером, в нём архив `statecraft-lectures-1.1.0.zip` (12 PDF,
5,9 МБ) и перечень лекций, порождённый из реестра `course.typ`.

**Второй выпуск — 1.2.0**, тег `v1.2.0` от 18.08.2026: 14 PDF (12 лекций и
два приложения). Что вошло: приложение «Требования к коду курса» (§ 5.5),
отсылки к разделам у всех 105 контрольных вопросов (§ 5.2), синтез схемы
детектора `1101` и проверка UTF-8 как ДКА взамен единственного примера на
копейках (§ 5.6), разбор лекции 7 по современным источникам (§ 3.3).

**Третий выпуск — 1.3.0**, тег `v1.3.0` от 18.08.2026: 15 PDF. Главное в нём
— **сводный том**: весь курс одним документом (299 страниц в этом выпуске,
318 на 18.08.2026), с единым оглавлением, сквозной нумерацией и одним
списком литературы. Кроме него: подсветка листингов там, где её не было
(Promela, БНФ, вставки `#raw`),
подписи семи таблиц и четырёх листингов, оставшихся без них после
конвертации, разделение списков во фронт-материи и проверка подсветки по
собранным PDF (`make highlight`).

Выпуск дал ещё одно наблюдение, уже про сам конвейер: первая попытка упала
не на нашем коде — `api.github.com` ответил 503 при загрузке архива, когда
тег уже стоял. Повторный `gh release create` в такой ситуации падает с
«release already exists», то есть задание нельзя было просто перезапустить.
Шаг публикации переписан: существующий релиз дозаполняется через
`gh release upload --clobber`, попытка повторяется трижды. Правило общее —
**шаг, который что-то создаёт во внешней системе, обязан быть
перезапускаемым**, иначе один сетевой сбой требует ручного вмешательства.

**Четвёртый выпуск — 1.4.0**, тег `v1.4.0` от 18.08.2026: 15 PDF, 5,3 МБ.
Что вошло: приложение лекции 10 «Распознавание и копирование на машине
Тьюринга» (языки $a^n b^n$ и палиндромов, копирование $w arrow.r w w$ —
таблицы переходов сняты с работающих машин практикума), раздел лекции 1
«Откуда взялась дисциплина» (две родословные предмета и три «этажа» курса
таблицей), раздел лекции 7 «Автомат и его окружение» (интерфейсы вместо
железа и четыре источника входов) — и вёрстка: выключка по формату,
отступы заголовков и центрированные служебные разделы, вынесенные в
`heading-gaps` и `service-heading-align`.

**Пятый выпуск — 1.5.0**, тег `v1.5.0` от 18.08.2026: 20 PDF. Главное —
**лабораторные работы**: пять заданий к лекциям 3, 4, 6, 7 и 10, каждое с
22–30 вариантами, критериями приёмки и машинной проверкой каждого шага. Вместе
с ними в шаблоне появился третий вид документа (`lab-id`, реестр `labs` в
`course.typ`), а в заметках к релизу — раздел «Лабораторные работы»
(`course.sh --labs`). Кроме них: точка входа у лексера (`06-lexical-analyze`)
взамен заглушки и два дефекта, которые она вскрыла, — нулевая длина
односимвольных лексем и недостижимая ветвь, приписывавшая закрывающей скобке
вид умножения; в лекции 6 заменена грамматика, которая приписывалась этому
практикуму, а описывала другой язык.

**Шестой выпуск — 1.6.0**, тег `v1.6.0` от 18.08.2026: 20 PDF. Что вошло:

- **лабораторные работы включены в сводный том** приложениями В--Ж (337
  страниц): том задуман полным комплектом курса, и задание — его часть;
- **разбивка листингов**. На девяти страницах тома код печатался поверх
  подписи и колонцифры: `raw` разрывается между страницами, а `figure` вокруг
  него — нет. Плюс номер строки стал колонкой `grid`, и перенос длинной
  строки больше не уходит под номера;
- **узоры клеточных автоматов стали векторными**: правила 90, 110 и 30
  рисовались знаками `.` и `#` прямо в тексте, хотя SVG уже порождались
  практикой и лежали в комплекте;
- `Description.md` приведён в вид README курса, задачи по машине Тьюринга из
  него перенесены в приложение лекции 10 (стало 22), модели Promela
  подключены к `ctest`, у лексера появилась точка входа.

Отдельно: **история репозитория вычищена от двоичных файлов** — `.git`
похудел с 72 до 24 МБ. Удалены растровые картинки лекций (34 МБ, заменены
векторными ещё в ТД-7), чужие PDF и старые сборки (19 МБ), дампы компилятора
и 54 бэкапа SimInTech; скринкаст и сборка SPIN под Windows оставлены. Ветки и
теги переписаны, релизы уцелели. Резервная копия прежней истории —
`~/statecraft-before-cleanup-20260818.bundle`.

Ловушка на будущее: `git rev-list --objects` показывает каждый объект **один
раз**, под первым найденным путём, поэтому файлы, переезжавшие между
каталогами, после первого прохода `filter-repo` остались в истории под старым
путём. Список путей надо брать из `git log --name-only`, а чистку повторять до
тех пор, пока двоичных путей вне рабочего дерева не останется ноль.

**Седьмой выпуск — 1.7.0**, тег `v1.7.0` от 19.08.2026: 20 PDF. Что вошло:

- **составные рисунки стали адресуемыми**: кадры внутри порождённых SVG и
  части, собранные в typst, помечаются буквами «а)», «б)», … по ГОСТ 7.32, а
  подпись расшифровывает каждую часть. Переписаны все составные рисунки
  лекции 11 и схема шаблонов Томпсона в лекции 5;
- **автомат из 17 состояний рисуется цветом**: номера вершин там не читались,
  и вместо них — заливка;
- **класс MISRA 10.x**: исправлены сдвиги знаковых значений, проверки
  вместимости через сложение и чтение частично заполненного буфера; граница
  («счётчик анализатора — плохая цель») записана в `docs/misra-report.md`;
- `pretty` оставляет в каталоге только PDF под читаемыми именами, и в релиз с
  артефактами CI по построению попадают только они.

Осталось: переносимость графики на Windows и поведение на HiDPI по-прежнему
не проверены — запускать это в GitHub Actions негде.

Ещё замечание на будущее: Takt-часть практикума (`12-takt`, `21-additional`)
проверяется только заданием `gui`, а оно факультативно. Регрессия в моделях
конвейер не остановит — как и случилось с SE-107, найденным лишь потому, что
задание всё же прогонялось.

### ТД-4. Документирование кода практикума — закрыт 17.08.2026

Снабдить код `practices/` комментариями в формате Doxygen. Объём — минимально
необходимый: комментарий обязан объяснять **зачем** существует сущность и
какого контракта от неё ждут, а не пересказывать, **как** она устроена.

Что документировать:

- каждый публичный заголовок — `@file` с назначением модуля и его местом в
  курсе (к какой лекции относится, что демонстрирует);
- каждую функцию публичного API — назначение, смысл параметров, что означает
  возвращаемое значение (особенно `false`/`NULL`), предусловия и владение
  памятью;
- типы и перечисления — что моделирует тип, чем различаются варианты
  перечисления;
- неочевидные решения — почему сделано именно так: ограничение C90, обход
  чужого дефекта, требование строгого режима компиляции.

Чего не делать:

- не повторять кодом по-русски (`/* увеличиваем счётчик */`);
- не документировать статические функции, смысл которых виден из имени;
- не заводить `@brief`/`@param` там, где сигнатура и имя уже всё сказали, —
  пустой тег хуже его отсутствия.

Инфраструктура: `Doxyfile` в корне, цель CMake `docs` (мягкий пропуск, если
`doxygen` не установлен), задание в CI. Комментарии в коде остаются на
русском языке, как и весь курс; `INPUT_ENCODING = UTF-8`.

#### Что сделано 17.08.2026

Инфраструктура заведена: `Doxyfile`, `scripts/build-docs.sh`, цель CMake
`docs` и задание CI. В задании стоит `--strict`: мягкий пропуск там означал
бы зелёное задание, ничего не проверившее.

Документированы **все 33 публичных заголовка** практикума и точки входа тех
практик, где заголовков нет (`07-simple-program`, `12-takt`). Проверка
разметки — на самом doxygen: `WARN_AS_ERROR` поднят, поэтому неверное имя в
`@param`, ссылка в никуда или незакрытая группа роняют сборку документации.

Записывалось то, что иначе приходится узнавать наощупь: что означают `false`
и `-1` у каждой функции, какие пределы у массивов и что бывает при их
достижении, кому принадлежит память, какие поля структуры пересчитываются
самим модулем. Отдельно — места, где код расходится с наивным ожиданием:
`base_types.h` с предупреждением о раскладке `bool`, `Implicant` с
требованием обнулять структуру целиком, `subset_last_visited` как глобальный
отладочный счётчик.

Ловушки самого doxygen, найденные по дороге:

- `@param` — либо для всех параметров функции, либо ни для одного: частично
  описанная сигнатура считается ошибкой;
- ссылка `#ИМЯ` разрешается только на **документированную** сущность, а при
  `HIDE_UNDOC_MEMBERS = YES` недокументированный макрос ссылкой не является;
- обратная косая черта — начало команды: регулярные выражения в комментариях
  пришлось заключить в `@code` … `@endcode`;
- `#include` внутри комментария разбирается как ссылка на `include`.

Заодно исправлено при чтении кода: опечатки в комментариях
`03-control-program` («конвейфере», «полодения», «удет») и неверный номер
лекции в шапке `12-takt/main.c` (была 11). Остальные номера лекций сверены с
именами каталогов — расхождений нет.

Две ловушки вылезли только в CI, потому что на машине разработчика окружение
богаче:

- doxygen 1.9 **не создаёт вложенный путь** `build/docs`, если каталога
  `build` нет, и падает с «Output directory does not exist». Локально каталог
  оставался от сборки;
- сборка doxygen в Debian включает `HAVE_DOT` по умолчанию, и **без graphviz
  документация не собирается вовсе** — каждый граф включений даёт ошибку.
  Графы отключены явно: пользы от них в учебном коде немного, а зависимость
  лишняя.

Правка комментариев затронула и лекции: листинги — копии файлов практикума,
поэтому после неё обязателен `make sync`, а у листингов с `from`/`to`
диапазоны съезжают (в лекции 12 драйвер сдвинулся на строку `@file`).

### ТД-5. Проверка примеров на соответствие MISRA C — закрыт 17.08.2026

Проверка **факультативная**: код курса пишется на ISO C90 и учит автоматному
программированию, а не сертификации, поэтому MISRA здесь — инструмент оценки,
а не обязательное требование. Смысл задачи в том, чтобы показать студенту, как
выглядит промышленный набор правил для встраиваемого кода и какие места
учебных примеров он ставит под вопрос.

Что сделать:

- выбрать и зафиксировать редакцию правил (MISRA C:2012 с поправками либо
  MISRA C:2004 — ближе к C90, на котором написан практикум) и **источник
  описания**: официальная публикация MISRA Consortium, а при её отсутствии —
  открытые наборы правил анализаторов (`cppcheck --addon=misra`,
  `clang-tidy`), с явным указанием, что это переложение, а не первоисточник;
- завести цель `misra-check` (мягкий пропуск, если анализатор не установлен)
  и факультативное задание CI, не влияющее на итог сборки;
- прогнать анализ по всем каталогам `practices/` и собрать отчёт.

Отчёт (`docs/misra-report.md`) обязан содержать:

+ **порядок проверки** — какой анализатор, какой версии, с какими ключами и
  на какой редакции правил; как воспроизвести прогон одной командой;
+ **источник описания правил** — на что опирались формулировки, со ссылками;
+ **что проверено** — перечень примеров практикума с числом файлов и строк;
  явно назвать то, что из проверки исключено (заголовок Catch2, сгенерированный
  SimInTech код, сборки SPIN) и почему;
+ **что найдено** — таблица «правило → место → суть нарушения», сгруппированная
  по правилам, а не по файлам; рядом — оценка, дефект это или сознательное
  отступление ради наглядности учебного примера;
+ **исправления, риски и приоритеты** — что исправлено, что оставлено и
  почему; чем рискует код в каждом оставленном случае; очерёдность работ.

Правило для последнего раздела: приоритет ставится по риску в реальной
эксплуатации (неопределённое поведение, переполнение, работа с указателями),
а не по числу срабатываний анализатора. Учебная наглядность — законная
причина отступить от правила, но она должна быть записана.

#### Что сделано 17.08.2026

Отчёт — [docs/misra-report.md](docs/misra-report.md). Прогон:
`./scripts/check-misra.sh` (cppcheck 2.21 с аддоном `misra`, `--std=c89`),
цель CMake `misra-check`, факультативное задание CI с `continue-on-error`.
Редакция — **MISRA C:2012**: она дальше от C90, чем редакция 2004 года, но
открытых переложений набора 2004 года в анализаторах нет, а отчёт обязан
воспроизводиться.

Оба использованных источника — переложения, и в отчёте они названы своими
именами: классификация берётся из аддона cppcheck, формулировки правил
написаны для курса заново (`docs/misra-rules.txt`). Официальную публикацию
MISRA распространять нельзя, поэтому отчёт утверждает «анализатор относит
это место к правилу N.M», а не «код нарушает правило N.M».

**Итог прогона: 3572 нарушения на 11 924 строках** (cppcheck 2.21; в CI, где
`apt` ставит 2.13, тех же нарушений 2820 — полнота проверок аддона зависит от
версии, и сравнивать имеет смысл только прогоны одной) — и это не приговор коду,
а следствие постановки: MISRA запрещает печать, ранний возврат и
динамическую память, без которых учебного примера не существует. Разбор по
классам — в отчёте; главное, что стоит помнить:

- четыре правила дают 60 % всех срабатываний (`15.5` единственный выход,
  `17.7` неиспользуемый результат, `21.6` стандартный ввод-вывод, `12.1`
  скобки) и все четыре — сознательные отступления;
- 41 срабатывание правила `17.3` — **шум анализа**: анализатор не видит ни
  системных заголовков сокетов, ни порождённого `taktc` заголовка;
- класс `10.x` разобран 19.08.2026 (см. отчёт, раздел «Класс 10.x»).
  Исправлено то, где правило указывает на ловушку языка, а не на стиль:
  сдвиги знаковых значений (`1 << n` — неопределённое поведение при
  переполнении), проверки вместимости через сложение (`total + 1 > capacity`
  переполняется и перестаёт защищать), сравнения беззнакового с нулём и
  чтение частично заполненного буфера. Счётчик при этом сдвинулся мало
  (220 → 200), и это главный урок: **счётчик анализатора — плохая цель**.
  Остаток — перечисления как целые, `size_t` на границе с `int` и
  приведения, порождённые самими исправлениями (`10.1` меняется на `10.8`);
  чтобы убрать их, пришлось бы вести беззнаковые типы сквозь все интерфейсы
  курса, а это уже другой курс.

**Исправлено по ходу разбора** — пять мест, из них два с настоящим риском:
несоответствие спецификатора `printf` типу аргумента (`%lu` для `size_t`,
`%d` для `unsigned`) в `sensor.c` и `emulate_network_cli.c` — неопределённое
поведение, которое на 32-разрядной платформе даёт неверную печать; и
указатели на строковые литералы, объявленные как `char *`. После правок
других находок самого cppcheck (вне MISRA) не осталось.

Правило на будущее, ради которого задача и была факультативной: **учебная
наглядность — законная причина отступить, но она записывается**. Каждое
оставленное отступление в отчёте имеет строку с оценкой риска.

### ТД-6. Вычитка и доработка справки по Git — закрыт 17.08.2026

Справка по Git (286 строк) перенесена из каталога `02.VerControlSystem`
16.08.2026 как есть — её содержимое ни разу не проверялось.

Проверить и исправить:

- **фактические ошибки**. Уже видно: Ларри Маквой назван «сам разработчик
  Linux» (он автор BitKeeper и основатель BitMover, к разработке ядра
  отношения не имел); стоит перепроверить и остальные утверждения
  исторической справки — даты апреля—июля 2005 года, передачу сопровождения
  Дзюнио Хамано, обстоятельства отзыва лицензии BitKeeper;
- **опечатки и стиль**: «принцип паботы», «почитать с татье», `WiKi`;
  дефис вместо тире, обратные кавычки вокруг слов, которые терминами не
  являются;
- **актуальность команд**: документ писался до широкого распространения
  `git switch` и `git restore`, ветка по умолчанию местами `master`;
  проверить каждую команду фактическим запуском, а не по памяти;
- **чего не хватает** для курса: порядок сдачи лабораторных (ветка на работу,
  оформление коммита, pull request), разрешение конфликтов, `.gitignore`,
  что делать с бинарниками — в репозитории курса эта проблема уже есть.

Изображения: три растровых схемы (`git_other_vcs_data.png`,
`git_inter_data.png`, `git_local_operations.png`) перерисовать в векторном
виде. Источник изображений — книга Pro Git (CC BY-NC-SA), поэтому перерисовка
нужна ещё и по лицензионным соображениям; в подписи указать происхождение
схемы. Инструмент — тот же, что в лекциях: CeTZ через `template/diagrams.typ`,
с выгрузкой в SVG, либо прямая вёрстка схем в typst, если документ решат
перевести из Markdown в общий шаблон курса.

Необходимо сделать его приложением к курсу на typst.

#### Что сделано 17.08.2026

Справка стала **приложением курса**: `lectures/src/a1-version-control/`,
13 страниц, собирается той же командой, что и лекции, и попадает в комплект
`make pack`. Markdown-версия и три растровые схемы удалены.

Как это устроено. В `course.typ` появился второй реестр — `appendices`
(id, название, дата), рядом с реестром лекций: единственный источник правды
остался один. Шаблон печатает приложение тем же кодом, что и лекцию, но
вместо «Лекция N» на титуле и в колонтитуле стоит «Приложение» — за это
отвечает параметр `appendix-id`. Скрипты (`build.sh`, `check.sh`) собирают и
проверяют приложения наравне с лекциями, читаемое имя PDF —
«Приложение — Система управления версиями Git.pdf».

**Исторических ошибок оказалось четыре, и все они названы в самом
приложении** — врезкой, а не молча: Ларри Маквой был представлен «самим
разработчиком Linux» (он автор BitKeeper и к ядру отношения не имел);
7 апреля 2005 года Git не «управлялся неготовой системой», а начал хранить
собственную историю; 26 (а не 25) июля Торвальдс передал Дзюнио Хамано
сопровождение **Git**, а не отказался от обязанностей в Linux; версия 1.0
вышла 21 декабря, а не 7 апреля. Даты сверены по @Chacon_ProGit и хронологии
проекта.

**Схемы перерисованы** средствами CeTZ (`scheme` из `template/diagrams.typ`),
в подписи каждой указано происхождение: «перерисована по Pro Git». Это и
лицензионная необходимость (книга под CC BY-NC-SA), и качество: схемы стали
векторными, как остальные рисунки курса. Заодно удалён
`docs/github-git-cheat-sheet.pdf` — чужой файл, распространявшийся вместе с
курсом без указания лицензии; вместо него в приложении ссылки на источники.

**Дописано то, чего не было:** разрешение конфликтов (с разметкой файла и
порядком действий), `.gitignore` и что делать с уже отслеживаемым файлом,
двоичные файлы (здесь же честно сказано, что репозиторий курса — пример того,
как делать не надо: в истории лежат `spin.exe`, скринкаст на 21,5 МБ и
шестьдесят резервных копий SimInTech), порядок сдачи лабораторных работ
(ветка на работу, оформление коммита, pull request, исправление замечаний).

**Команды приведены к современному Git и проверены запуском** на версии 2.55:
заведён репозиторий, сделаны коммиты, ветки, слияние с конфликтом, `stash`,
`.gitignore`, `rm --cached`, обмен с локальным «сервером» (`clone`, `push`,
`pull --rebase`, `force-with-lease`). Добавлены `git switch` и `git restore`
с объяснением, зачем их отделили от `git checkout`; отмечено расхождение
`master`/`main` и как его убрать настройкой `init.defaultBranch`.

### ТД-7. Сканы иллюстраций из книг — закрыт 17.08.2026

В лекции 11 лежит 21 МБ растровых иллюстраций (`src/11-cells/images/Life_*.png`)
— это сканы страниц книги М. Гарднера. Проблем две, и обе стоит закрыть до
раздачи курса.

- **Лицензия.** Сканы чужой книги нельзя распространять вместе с курсом.
  Ровно та же причина, что в ТД-6 с иллюстрациями Pro Git.
- **Качество.** Это фотографии страниц с серым фоном и артефактами печати;
  рядом с векторными диаграммами остальных лекций они выглядят чужеродно.

Что делать: конфигурации «Жизни» (устойчивые фигуры, глайдер, ружьё Госпера,
«пожиратель», «Сад Эдема», агарь) не перерисовывать вручную, а **порождать
программой** — практика `practices/11-cells` уже печатает поля в текстовом
виде, и в лекции 11 такие распечатки уже используются для правил Вольфрама и
муравья Лэнгтона. Нужно добавить в практику режим вывода конкретных
конфигураций и заменить сканы на порождённый текст либо на векторную
отрисовку по тем же данным.

**Сделано 17.08.2026.** В `practices/11-cells` появился модуль `render.c` —
векторный вывод (SVG) без внешних зависимостей, а в CLI подкоманда `svg`:
ленты поколений «Жизни», сцена «пожиратель и планер», поле муравья Лэнгтона,
прогон муравья по тропе Санта-Фе, узор Вольфрама и диаграмма автомата по его
строковой записи. Весь набор картинок лекции перерисовывается командой
`practices/11-cells/render-figures.sh`; файлы лежат в
`lectures/src/11-cells/images/*.svg` (600 КБ на 28 файлов).

**ASCII-рисунки заменены векторными (18.08.2026).** Три места лекции 11
показывали узоры знаками `.` и `#` прямо в тексте: узор правила 90, узор
правила 110 и поле муравья Лэнгтона. Векторные картинки для них уже
порождались практикой (`wolfram-90.svg`, `wolfram-110.svg`, `wolfram-30.svg`)
и просто не были включены в лекцию. Теперь включены, ASCII убран; узор
правила 30 добавлен там, где разбирается класс III. У муравья текстовая копия
удалена — векторное поле стоит абзацем выше, и дублировать его знаками
незачем. Проверка та же, что и раньше: `make highlight` считает одноцветные
листинги, их стало 40 вместо 49, и все оставшиеся — вывод программ, а не
рисунки.

**ТД-7 закрыт для лекции 11.** Все девять сканов книги заменены порождёнными
картинками и удалены; каталог `images/` похудел с 22 МБ до 900 КБ (осталась
только шутка `Joke_automation_module_release.jpg`, к конфигурациям отношения
не имеющая).

Конфигурации, которых в практике не было, **считаны с самих сканов** и
проверены прогоном по тому, что о них сказано в книге:

| Конфигурация | Чем проверена |
|---|---|
| чеширский кот (18 клеток) | «улыбка» из 4 клеток на 6-м ходу, блок на 7-м |
| «Сад Эдема» Бэнкса (226 клеток) | размеры 33 × 9 и число клеток; отсутствие предшественника прогоном не проверяется — это утверждение о несуществовании |
| агар Уэйнрайта + «вирус» | вирус в углу четырёх блоков гибнет, агар восстанавливается за 2 хода; вирус у грани блока разрушает решётку без предела |
| пять триплетов | три погибают за 3 хода, тримино даёт блок, ряд — мигалку |
| размножение тримино | правило чётности Фредкина (окрестность фон Неймана): на 4-м ходу ровно 4 копии |

Правило чётности потребовало второй локальной функции переходов
(`life_step_parity`) — тот же автомат, то же поле, другое правило; заодно это
рабочая иллюстрация к разделу «Самовоспроизведение».

#### Остальные лекции разобраны 17.08.2026

**Растровых изображений в лекциях не осталось вовсе**: было 34 файла и
5,4 МБ в `lectures/src`, стало 1,9 МБ — только порождённые SVG практики 11 и
рисунки, которые строит сам документ. Каждый случай разобран отдельно, потому
что «удалить всё» — не решение: часть картинок несла содержание.

| Лекция | Что было | Что стало |
|---|---|---|
| 7 | три схемы построения автомата из работы Кузнецова | перерисованы на CeTZ; подписи, которые были пустыми, написаны |
| 7 | фотография рукописной схемы, 2,6 МБ | таблица обозначений, сверенная с кодом практики; диаграмма рядом уже была векторной (ТД-10) |
| 6 | чужая шпаргалка по регулярным выражениям с брендингом стороннего сайта | собственная таблица на русском + замечание о том, какая строка выводит за пределы регулярных языков |
| 6 | растровый НКА для `a*(cb\|c*)d` | `fsm-diagram` |
| 10 | 13 сканов программ-таблиц в задачнике | таблицы typst (`mt-program`); каждая распознана и набрана заново |
| 10 | снежинка Коха картинкой | строится кодом лекции: то же правило, что описано в тексте |
| 10 | две фотографии Тьюринга без указания источника | удалены; текст самодостаточен |
| 1 | скриншот индекса TIOBE | ссылка: рейтинг меняется ежемесячно, снимок устаревает к моменту чтения |
| 2, 11 | три мема | удалены: стояли с пустыми подписями и учебной нагрузки не несли |
| 6 | логотип Raku и пять неиспользуемых файлов | удалены |

Правило, которое из этого следует: **рисунок в лекции либо порождается кодом
(практикой или самим документом), либо перерисовывается средствами шаблона с
указанием происхождения**. Чужой растр не проходит ни по лицензии, ни по
качеству, ни по сопровождению: он молча расходится с текстом, который
правится.

## Практикум

`practices/` — часть общей сборки. Каждая цель проходит через
`statecraft_strict_c()`: ISO C90 без расширений, `-Wall -Wextra -pedantic`
и `-Werror` (отключается `-DSTATECRAFT_STRICT=OFF`). Вместо `<stdbool.h>` и
`<stdint.h>`, которых в C90 нет, подключается `common/base_types.h`. Тесты —
Catch2, зарегистрированы в `ctest`.

Практики лекций 5 и 4 соединяются в конвейер и дают сквозной пример курса:
`05-kleene "(a|b)*abb" | 04-dfa-nfa --minimize` — 14 состояний по Томпсону,
5 после детерминизации, 4 после минимизации. Общее представление автоматов —
`common/fsm.[ch]` (структуры и ввод-вывод), алгоритмы живут в самих практиках.

Подпроекты, на которые ссылаются лекции:

Каталоги названы `<номер лекции>-<тема>` в kebab-case; полный список и
соответствие лекциям — `practices/README.md`.

| Каталог | Что демонстрирует | Лекция |
|---|---|---|
| `01-words` | слова и языки: операции, итерация с ограничением длины | 1 |
| `02-fsm-delay` | автомат Мили «Задержка» + модель SimInTech | 2 |
| `03-synthesis` | синтез схемы по таблице переходов, Квайн — Мак-Класки | 3 |
| `03-control-program` | линия точечной сварки: автомат отделён от установки тремя интерфейсами, эмуляция из файла и по сети | 7 |
| `04-dfa-nfa` | детерминизация и минимизация, вывод Graphviz | 4 |
| `05-kleene` | конструкция Томпсона: РВ → ε-НКА | 5 |
| `06-regular-expression`, `06-lexical-analyze` | движок регулярных выражений и лексер | 6 |
| `07-simple-program` | вложенные switch/if как контрпример | 7 |
| `07-fsm-insert-element` | автомат поиска вставки, 3 состояния | 7 |
| `07-three-ways` | один автомат тремя способами, покрытие переходов | 7 |
| `08-fsm-control-pneumo` | циклограмма пневмоцилиндров, кодогенерация SimInTech | 8 |
| `08-bulk-loading` | загрузка сыпучих материалов: автомат и модель установки | 8 |
| `09-promela` | 10 примеров Promela от HelloWorld до семафоров | 9 |
| `10-turing-machine` | интерпретатор машины Тьюринга | 10 |
| `11-cells` | клеточные автоматы, тропа Санта-Фе, векторные картинки лекции | 11 |
| `11-ant-search` | поиск автомата муравья: (1 + 1)-ЭС и эволюционное программирование | 11 |
| `12-takt` | модели на языке Takt, порождение C, драйвер | 12 |
| `20-welding-line` | сквозной проект: линия сварки, покрытие переходов, экспорт в Promela | 3, 7, 8, 9 |
| `21-additional` | погрузчик цеха: управление на Takt, планировщик и модель установки на C90 | — |

Файлы `practices/08-fsm-control-pneumo/generated/*.inc` и `*.log` — в
**CP1251**, не UTF-8. В `lectures/src/08-statecharts/code/generated/` лежат
перекодированные копии. В `generated/PneumoAutomate.h` вшит абсолютный
windows-путь `E:\GitHub\automata_programming\...`.

### ТД-8. Перенос материала из репозитория `c_fsm`

Репозиторий <https://github.com/BasePractice/c_fsm> — 12 модулей реализаций
автоматов на C, часть с GUI на raylib. Проверен 17.08.2026: клонирован,
собран, разобраны модули `clever_ant` и `game_life`.

#### Что стоит перенести

| Из `c_fsm` | Куда | Зачем |
|---|---|---|
| `clever_ant/ant.c` — **каноническая тропа Санта-Фе** (89 яблок, 38 клеток разрыва, поле 32×32) | `practices/11-cells` | заменить нашу учебную тропу: результаты станут сравнимы с литературой |
| `clever_ant/clever_ant.c` — автомат из 5 состояний, проходит тропу за **315 шагов** (проверено прогоном) | `practices/11-cells` | эталон для сравнения стратегий |
| `clever_ant/clever_ant_gen.*` + `clever_ant_gen.txt` — поиск автомата и журнал улучшений: 315 → 295 → … → **181 шаг** (17 состояний) | новая практика к лекции 11 | ровно то, о чём лекция говорит словами: «как такой автомат ищут» |
| `regexp/regexp_isbn.c`, `regexp_tags.c`, `regexp_nums_*.c` | `practices/06-*` | прикладные ДКА: проверка ISBN, разбор тегов, числовые форматы |
| `fsm/fsm_bulk.c` — управление сыпучими материалами, 9+ состояний, регистры входов/выходов, таймеры | лекции 7–8 | пример промышленного размера, которого в курсе нет |
| `fsm/transport_loader.c` + `factory.json` — транспортировка с поиском пути (BFS) по конфигурации | лекция 8 или сквозной проект | автомат плюс планирование; конфигурация вынесена в JSON |
| `fsm/fsm.tex` | сверить с лекциями 2–3 | ещё один источник по автоматам курса |
| GUI на raylib (`*/gui/`) | **под вопросом** | зависимость тяжёлая, практикум сейчас консольный и C90; решать отдельно |

Чего переносить не нужно: `chess`, `forth`, `microc` — вне предмета курса;
`turing`, `pneumo_ctrl`, `fsm_coin`, `fsm_delay`, `fsm_insert`,
`fsm_welding`, `math_lang` — их аналоги в практикуме уже есть, но сверить
поведение полезно.

#### Сверка поведения аналогов (17.08.2026)

Последний пункт закрыт: `c_fsm` собран заново, и каждый модуль прогнан рядом
с нашей практикой на одних входах. Сверка нашла **три дефекта переноса —
все наши**, и это главный её итог: расходились не чужие программы, а наши
копии.

| Модуль | Как сверено | Итог |
|---|---|---|
| `fsm_delay` | построчное сравнение | код совпадает буква в букву |
| `fsm_coin` | 40 тактов случайной последовательности монет; выход и состояние сверены с таблицей `machine/coin.fsm` | расхождений нет, покрыты все 12 клеток таблицы лекции 3 |
| `fsm_insert` | прогон на семи парах последовательностей | **найден наш дефект**: при первой последовательности короче второй граница вставки выдавалась как `m_2_len - 1`, а на остальных путях — как `m_2_len`; один автомат сообщал границу двумя способами. Тест закреплял неверное значение |
| `pneumo_ctrl` | потактовая сверка на сценарии оригинала | **найдены два наших дефекта**: шаг выдержки в режиме симуляции равен 1 вместо 5 (автомат шёл в пять раз быстрее датчиков и уходил в аварию) и переставленный порядок печати «вверху/внизу». После правки последовательности состояний совпадают тождественно: каждое состояние держится 6 тактов, порядок 1→…→9→1 |
| `turing` | прогон двух программ | вывод совпадает; третьей программы (`turing_direct_remove`) у нас нет, и переносить нечего: она печатает вход неизменённым, назначение из кода не восстанавливается |
| `math_lang` | оба лексера собраны с общим драйвером и прогнаны на девяти выражениях, включая ошибочные | последовательности лексем совпадают полностью |
| `fsm_welding` | чтение и прогон | **аналога нет**: у них управление сварочным аппаратом (ток, напряжение, подача проволоки, газ, насыщение и спад), у нас — линия точечной сварки. Совпадает только название |

Побочный результат: у практики `08-fsm-control-pneumo` **не было входных
данных вовсе** — в каталоге лежат только двоичные протоколы SimInTech, и
запустить её было нечем. Сценарий перенесён из `c_fsm`, драйвер научен
пропускать комментарии в строках, добавлен тест `ctest`.

Дефекты оригинала, замеченные попутно: `fsm_coin` не проверяет конец ввода и
на EOF печатает ошибку бесконечно; `fsm_insert` при первой последовательности
короче второй читает за границей массива — и «находит» совпадение на нулевых
байтах вне строк (наш вариант завершает разбор корректно).

#### Дефекты, найденные при разборе

Переносить код как есть нельзя: в обоих разобранных модулях есть ошибки.

**`game_life/game_life.c`, функция `around_the_cell` — тор не работает.**
Индексы соседей объявлены как `size_t`, а вычисляются из `int`, который для
левого и верхнего края равен −1:

```c
size_t iy, ix;
iy = ity;                 /* int -1 -> size_t SIZE_MAX */
if (iy < 0) { ... }       /* всегда ложь: тип беззнаковый */
else if (iy >= w->height) iy = 0;
```

Проверка `iy < 0` для беззнакового типа тождественно ложна, поэтому вместо
`height - 1` подставляется 0. Проверено экспериментом: у клетки (0,0) на поле
8×8 «соседями» оказываются `(0,0)` **четыре раза**, `(0,1)` и `(1,0)` — по
два раза. То есть клетка считает соседом саму себя, а противоположный край
поля не учитывается вовсе. Все клетки на левой и верхней границе живут по
неверным правилам. Компилятор предупреждает об этом (`-Wsign-compare`), но
предупреждения в проекте не включены как ошибки.

Там же: `w->output_cells` не инициализируется в `init_world`, а читается в
`poll_world` — чтение неинициализированной памяти; `destroy_world` не
закрывает `w->output`, открытый через `fopen`, — утечка дескриптора;
`around_the_cell` возвращает указатель на `static` массив (сама переменная
названа `not_thread_safe`); `assert(x >= 0)` при `size_t x` бессмысленно.

**`clever_ant/clever_ant.c` — нет ограничения на число шагов.** Цикл
`while (apples > 0)` не завершится, если автомат не съедает всё; счётчик
`steps` типа `uint16_t` при этом молча переполнится. В канонической
постановке лимит — 600 тактов, и он же служит критерием сравнения стратегий.
Кроме того: число яблок задано дважды (`APPLES` в `ant.c` и локальная
переменная `apples = 89`) — при правке карты они разойдутся; всё состояние
автомата в глобальных переменных, поэтому два прогона подряд в одном
процессе невозможны, а тестировать нечем; `assert(false)` в `has_food()`
исчезает в Release вместе с `NDEBUG`, и функция молча возвращает `false` —
ровно тот случай, о котором в разделе «Проверка 16.08.2026» сказано «assert
не заменяет return».

#### Порядок работ

+ заменить учебную тропу в `practices/11-cells` на каноническую, перенеся
  карту из `ant.c` (сама карта корректна — 89 яблок проверено подсчётом);
+ проверить, проходит ли её наш эталонный автомат из шести состояний; если
  нет — разобрать, на каком разрыве встаёт, и это же оформить задачей;
+ перенести автомат из пяти состояний как второй эталон (315 шагов);
+ перенести поиск автомата (`clever_ant_gen`) отдельной практикой, приведя
  код к требованиям курса: без глобального состояния, с лимитом шагов, с
  тестами;
+ каждый перенос сопровождать исправлением найденных дефектов и тестом,
  который их фиксирует, — иначе ошибки переедут вместе с кодом.

#### Что сделано 17.08.2026 (часть «умный муравей»)

- **тропа Санта-Фе перенесена** в `practices/11-cells/ant_trail.c`; 89 клеток
  еды проверяются тестом, а не принимаются на веру;
- **эталонный автомат из шести состояний Санта-Фе не проходит**: съедает
  59 из 89 и зацикливается, уйдя с тропы у поворота в строке 27. Он оставлен
  под именем `probe` как показательный случай «стратегия, подогнанная под
  знакомую карту»; в лекции об этом отдельная врезка;
- перенесены **автомат из пяти состояний** (`scan`, 315 тактов) и **автомат
  из журнала поиска** (`evolved`, 17 состояний, 181 такт). Весь журнал
  улучшений `clever_ant_gen.txt` (55 записей) воспроизведён потактово — наша
  модель прогона тождественна модели `c_fsm`;
- заведена практика **`practices/11-ant-search`** с двумя алгоритмами поиска:
  (1 + 1)-ЭС и эволюционное программирование по статье Angeline и Pollack
  (`--algorithm ep93`), на которую ссылался исходный код `c_fsm`
  (`clever_ant/ep93.pdf`). Дефекты оригинала (глобальное состояние,
  отсутствие лимита шагов, `assert` вместо `return`, дублирование числа
  яблок) исправлены и закрыты тестами;
- **сравнение алгоритмов** сведено в `practices/11-ant-search/README.md` и в
  лекцию: заявленного в статье ускорения от заморозки модулей воспроизвести
  не удалось (медиана без заморозки вдвое меньше). Разбор причины — способ
  сравнения в статье: отсортированные ряды вместо одинаковых условий, два
  неудачных прогона исключены из таблицы;
- у прогона появился **снимок** (`ant_trail_snapshot`) с пройденным путём —
  на нём построены иллюстрации лекции.

#### Что сделано 17.08.2026 (часть «прикладные регулярные выражения»)

Три распознавателя из `c_fsm/regexp` перенесены в `practices/06-regular-expression`
модулем `applied_dfa.[ch]` — но не лестницами `switch`, как в оригинале, а
**таблицей переходов**: автомат стал данными, прогон — общей функцией.

| Формат | Выражение | Состояний |
|---|---|---|
| номер книги | `(ISBN:?)?\d{9}[\dxX]` | 16 |
| парный тег | `<(kill\|pop)>.*</\1>` | 30 |
| число | `[+-]?\d+(\.\d+)?([eE][+-]?\d+)?` | 8 |

Вместо двух числовых головоломок из `regexp_nums_*.c` (наборы цифр без
внятного смысла) взят практический формат вещественного числа.

Найденное при разборе и вынесенное в лекцию:

- **обратная ссылка** `^<(kill|pop)>.*<\/\1>$` в общем случае выводит за
  пределы регулярных языков, но здесь имён ровно два, поэтому язык регулярен —
  автомат содержит две ветви. В оригинале имя тега хранится в массиве, то есть
  автомат дополнен регистром и конечным уже не является;
- **закрывающий тег нельзя искать наивно**: при несовпадении возвращаться надо
  не в «читаю тело», а в состояние наибольшего совпавшего суффикса (приём
  Кнута — Морриса — Пратта). Наивный автомат из `c_fsm` ошибается на входе
  `<kill><</kill>`; тест это фиксирует;
- дефекты оригинала: пропущенный `break` в `regexp_isbn.c` (провал в соседний
  `case`) и непроверенный `>` после имени тега в `regexp_tags.c` — строка
  `<killZ` принималась как открывающий тег;
- приставка EAN `(97[89])?` не перенесена намеренно: она удваивает таблицу
  (нужно вести обе трактовки первых трёх цифр и различать их длиной) — это
  задание лекции о цене необязательной группы в ДКА.

Заодно у практики появился CLI (`main.c` был заглушкой): список
распознавателей, прогон строк с указанием места расхождения и печать таблицы
переходов.

#### Что сделано 17.08.2026 (часть «сыпучие материалы»)

`fsm/fsm_bulk.c` перенесён практикой **`practices/08-bulk-loading`**: автомат
из одиннадцати состояний, восемь входов, семь выходов, выдержки и рецепт.
Автомат отделён от ввода-вывода (вход — регистр датчиков, выход — регистр
команд), а вместо файла с записанными входами заведена **модель установки**
(`bulk_plant.c`), которая отвечает на команды. Разница практическая: запись
приходится переснимать после каждой правки автомата, модель проверяет само
управление.

Дефекты оригинала, исправленные и закрытые тестами: закрытие заслонки
подавалось командой «открыть» (сигнал «закрыть» не использовался ни разу);
счётчик малых циклов рос без ограничения, а бит рецепта читался по нему;
сигналы большого круга объявлены и не используются; результат `fopen` не
проверялся; `print_state` писала в статический буфер.

Найдено прогоном на модели и в оригинале не проявлялось: **между малыми
циклами контейнер обязан возвращаться в исходное положение**. У автомата нет
координаты, только датчик «под резервуаром», а резервуары стоят слева друг за
другом — без возврата он гонит контейнер влево из позиции, которая уже левее
цели, и упирается в упор.

**`fsm/fsm.tex` проверен — переносить нечего.** Вопреки записи в таблице выше,
это не материал по автоматам, а 59-строчная заготовка LaTeX: преамбула,
обвязка `minted` для подсветки кода и один пример «Привет мир!». Ни автоматов,
ни текста лекции в файле нет.

**ТД-8 закрыт.** Оставшиеся пункты переехали в ТД-9: погрузчик
(`fsm/transport_loader.c` + `factory.json` + GUI) выносится в практику
`21-additional`, там же решается вопрос о raylib.

### ТД-9. Практика `21-additional` и вопрос о графике — закрыт 17.08.2026

Всё, что не ложится в конкретную лекцию, раньше либо оставалось в стороне,
либо тянуло за собой чужие зависимости. Задача требовала одного каталога для
такого материала и решения по графическим приложениям; итог — в подразделах
«Решение о языках и графике», «Что сделано 17.08.2026» и «Остальные модули
`c_fsm`».

#### Что вынести в `21-additional`

Каталог называется по тому же правилу, что и остальные (`20-welding-line` —
сквозной проект, `21-additional` — дополнительные примеры), и собирается той
же командой. Кандидаты:

- **погрузчик** из `c_fsm` (`fsm/transport_loader.c` + `factory.json` +
  `gui/transport_loader_gui.cpp`): автомат транспортировки с поиском пути
  (BFS) по конфигурации из JSON. К лекции 8 он привязан слабо — там
  циклограмма, а здесь планирование маршрута, — но как пример работы автомата
  с внешней конфигурацией он ценен;
- прочие примеры `c_fsm`, не привязанные к лекциям и не дублирующие
  практикум;
- графические приложения (см. ниже), если решение по ним будет «оставить».

Требование к каталогу: он собирается вместе со всем курсом и **не ломает
сборку**, если графических зависимостей в системе нет. То есть целями с
графикой управляет опция CMake, по умолчанию выключенная, а консольная часть
собирается всегда.

#### Вопрос о raylib

В `c_fsm` четыре модуля с GUI (`fsm`, `chess`, `game_life`, `clever_ant`), все
на **raylib** плюс вендоренный `raygui.h`. Прежде чем переносить, надо
ответить, нужна ли графика вообще и в каком виде.

Что говорит против raylib в этом курсе:

+ **язык.** Практикум — ISO C90 без расширений; `transport_loader_gui.cpp` —
  1500 строк C++ с классами, `std::function`, `std::vector`, лямбдами. Это
  не «код курса с картинкой», а отдельная программа на другом языке;
+ **сборка.** raylib подтягивается `find_package(raylib 2.0 REQUIRED
  /usr/local)` — жёсткий путь, конкретная версия. В CI (ubuntu + macOS)
  графических библиотек нет, GPU нет, окна открыть нельзя;
+ **сопровождение.** У курса уже есть векторный вывод без единой зависимости
  (`practices/11-cells/render.c`, SVG через `fprintf`), и он решает задачу
  «показать поведение автомата» для лекций.

Что говорит за графику: интерактивный прогон нагляднее статической картинки —
особенно для погрузчика, где интересен путь, и для «Жизни», где интересна
динамика.

Варианты, которые стоит сравнить, прежде чем выбирать:

+ **отказаться от GUI**: оставить консольный вывод и порождение SVG. Ноль
  зависимостей, работает в CI, годится для лекций; интерактивности нет;
+ **терминальная анимация** (ANSI-escape, без ncurses): десятки строк кода,
  работает везде, где есть терминал; для «Жизни» и муравья этого достаточно;
+ **SDL2 или raylib как необязательная цель**: интерактивность есть, но
  добавляется внешняя зависимость и вторая сборочная ветка;
+ **вывод в HTML/JS**: программа порождает страницу с записью прогона,
  зависимостей нет, интерактивность есть, но появляется второй язык.

Решение принимать по критериям курса: собирается ли офлайн, работает ли в CI,
на каком языке, сколько сопровождать. Оформить решение отдельной записью
здесь же, чтобы оно не пересматривалось при каждом переносе.

#### UI погрузчика: что проверить

Если решено переносить, `transport_loader_gui.cpp` требует отдельного разбора,
а не механического копирования:

+ **корректность.** Что именно рисуется и соответствует ли это модели: карта
  из `factory.json` кодируется числами (стены, стеллажи, проходы, точки), и
  надо убедиться, что отрисовка не расходится с тем, как ту же карту читает
  автомат. Разошедшаяся картинка хуже отсутствующей: она врёт про поведение;
+ **переносимость.** Проверить сборку и работу на macOS, Linux и Windows;
  отдельно — поведение при отсутствии дисплея (CI, ssh без X11): программа
  обязана завершаться с внятным сообщением, а не падать;
+ **разрешение и масштаб.** Размеры окна и шрифтов заданы константами;
  проверить на экранах с масштабированием (HiDPI) и малых разрешениях;
+ **язык интерфейса.** Курс русскоязычный, а raygui по умолчанию не имеет
  кириллических глифов — потребуется шрифт и его лицензия;
+ **отделение от логики.** Автомат и поиск пути не должны зависеть от
  графики: рисование поверх модели, а не модель внутри рисования. Это то же
  требование, что в `08-bulk-loading`, и по той же причине — иначе логику
  нельзя проверить тестом.

#### Решение о языках и графике (17.08.2026, согласовано с заказчиком курса)

Правило для `21-additional` и всего подобного материала:

+ **система управления — на языке Takt.** Автомат не пишется руками на Си, а
  описывается моделью и порождается `taktc`. Практика 12 показывает язык,
  здесь он работает на примере промышленного размера;
+ **всё остальное — чистый Си** (ISO C90, как везде в практикуме):
  конфигурация, планирование, модель установки, инструмент командной строки.
  Эта часть собирается всегда, даже когда компилятора Takt в системе нет;
+ **графика — C++ на raylib**, отдельной целью под опцией
  `STATECRAFT_BUILD_GUI` (по умолчанию `OFF`). В CI не собирается, на
  остальную сборку не влияет; при `ON` без raylib или без `taktc` цель
  пропускается с предупреждением.

Каталог практики разделён по составляющим: `loader/` — сам погрузчик,
`loader-gui/` — его графическая часть.

Три требования к графике, из которых первое — главное:

+ **приложение ничего не решает само.** Оно рисует состояние того же прогона
  (`loader/loader_runner.h`), который проверяется тестом в консольном
  драйвере. Копия логики внутри рисования — то, чем страдал оригинал, и
  расходящаяся с моделью картинка хуже отсутствующей;
+ **без дисплея — внятное сообщение, а не падение** (CI, ssh без X11);
+ **кириллица требует внешнего шрифта**: во встроенном шрифте raylib её нет.
  Шрифт ищется в системе или задаётся ключом `--font`, в курс не входит —
  распространять чужой шрифт вместе с примером нельзя (та же причина, что в
  ТД-1 и ТД-7). Не найден — подписи выводятся латиницей.

Переносить `transport_loader_gui.cpp` как есть всё равно нельзя: это 1500
строк C++, где логика перемешана с рисованием, и половина программы (автомат
`Parallel`) в нём вообще отсутствует.

#### Что сделано 17.08.2026

Практика `practices/21-additional` заведена; погрузчик перенесён.

**Автомата в оригинале не было.** `transport_loader_gui.cpp` подключает
готовый автомат `Parallel` из репозитория `fsmvm`
(`/Users/pastor/github/fsmvm/gen/c/position.c`, в `CMakeLists.txt` прописан
абсолютным путём), которого нет ни в `c_fsm`, ни в открытом доступе. Без него
графическое приложение не собирается вовсе. Поэтому система управления
написана заново — `model/loader.takt`, четыре параллельные модели:
`CommandReceiver`, `DriveController`, `LiftController`, `SafetyMonitor`.

**Консольная часть оригинала к конфигурации отношения не имеет.**
`transport_loader_main.c` читает `factory.json` и тут же его игнорирует: дальше
идёт поиск в глубину по вшитому лабиринту 10 × 10. Там же `uint8_t x = c.x +
DIRECTIONS[i][0]` при движении влево даёт 255, проверка `x >= 0` для
беззнакового типа тождественно истинна, а массив координат рассчитан на 50
элементов при 100 клетках поля — тот же класс дефекта, что в `game_life`.

Что сделано на C90: разбор конфигурации (`factory_map.[ch]`, подмножество JSON
вместо 76 КБ cJSON), планировщик (`route.[ch]`), модель цеха
(`loader_plant.[ch]`), инструмент (`main.c`), тесты. Связка автомата с
установкой (`loader_runner.[ch]`, C99, как в `12-takt`) одна на консольный
драйвер `loader_main.c` и на графику; собирается только при наличии `taktc`.

Найденное при разборе и вынесенное в README практики:

+ `PathLine::search` читает клетку **до** проверки границ — выход за пределы
  матрицы на каждой клетке у края карты; тест это фиксирует;
+ поиск ходил по любой непустой клетке слоя `paths`, не различая
  горизонтальные и вертикальные участки, хотя сам же их кодировал. Здесь
  правило работает: по участку — только вдоль него, поворот — только на метке.
  Это согласуется с конфигурацией: метки стоят на пересечениях;
+ **поворот стоит дороже клетки пути**, и поэтому поиск в ширину здесь не
  годится: нужна Дейкстра по состояниям «клетка + направление». Разница видна
  на добавленной конфигурации `factory-grid.json` (решётка 3 × 3): маршруты
  равной длины различаются числом поворотов;
+ прочитанная метка ни с чем не сверялась — погрузчик, промахнувшийся мимо
  поворота, продолжал ехать «по плану». В модели чужая метка — авария;
+ предела движения не было; `SafetyMonitor` поднимает аварию, если погрузчик
  едет дольше `BLIND_LIMIT` тактов, не встречая меток.

**Переработано по требованию заказчика (17.08.2026): физика — только с
датчиков.** Такт — шаг логики, его частота произвольна, поэтому ни одна
физическая величина не считается тактами. Автомат получает энкодер
(`sense_odometer`, см), дальномер (`sense_range`, см), акселерометр
(`sense_motion`), сканер места (`sense_stack`), сканер вил (`sense_pallet`) и
тензодатчик (`sense_load`); на них построены перебег между метками, остановка
перед препятствием, подтверждение начала движения и приём паллеты по кодам, а
не «по команде вслепую». Время осталось в одном месте — сторожевой таймер
команды (`CommandWatchdog`), и срок ему считает планировщик по длине перегона и
паспортной скорости (`LoaderTiming` в `route.h`); автомат срок только сторожит.
Модель установки (`loader_plant.c`) держит физику в сантиметрах и
миллисекундах и знает период такта — автомат его не знает.

Ещё две находки этой переработки:

/ Аварию замечают четверо (движение, вилы, сторож, таймер). Пока каждый сам
  поднимал и снимал признак, квитирование зависело от порядка тиков: одна
  модель снимала, другая в том же такте выставляла заново, и `reset` не
  работал. Признак отдан единственному владельцу — модели `FaultLatch`;
  остальные только просят (`fault_request`).
/ `taktc` не переводит в C константу `duration`, использованную в выражении
  выдержки (`after (deadline + MARGIN)` — ошибка CC-023); литерал
  (`after (deadline + 2s)`) работает.

**Проверено целиком** (17.08.2026, после установки `taktc`, `takt-sim` и
raylib): сборка Debug и Release без предупреждений в строгом режиме C90, 19
тестов ctest, прогон под `-fsanitize=address,undefined`, оформление кода,
компиляция модели, сценарий симулятора, прогоны драйвера и графическое
приложение. Модель, написанная по документации `BuT/book/src` и образцу
`examples/stacker.takt`, скомпилировалась `taktc` без единой правки — правки
понадобились вокруг неё, и они поучительны.

/ Рукопожатие требует обеих половин: драйвер снимал `cmd_valid` и в том же
  такте поднимал следующую команду. Автомат не успевал увидеть `cmd_valid = 0`,
  оставался в состоянии `Reporting` с поднятым `cmd_done`, а драйвер по этому
  сигналу досчитывал план до конца — при неподвижном погрузчике. Следующая
  команда теперь подаётся только после снятия `cmd_done`.
/ `taktc` заводит только нужные модели обработчики портов: числовых выходов у
  погрузчика нет, поэтому `write_numeric` в порождённой структуре не появился.
/ Порождённый заголовок не объявляет `extern "C"` — при сборке графики это
  даёт «templates must have C++ linkage». Включать его нужно внутри
  собственного блока `extern "C"`, а стандартные заголовки — до него.
/ Ошибку чтения конфигурации нашла картинка, а не тест: значение 13 в слое
  `map` считалось телом стеллажа, и на первом же снимке «стеллажи» оказались
  теми самыми проходами, по которым едет погрузчик (13 — покрытие проезда).
  Маршрут строится по слою `paths`, поэтому тесты этого не видели. Довод в
  пользу графики: она проверяет то, что тестом не покрыть.
/ Появились **задания** (`loader/mission/*.json`): доехать, взять паллету по
  коду, отвезти на свободное место, поставить. Модель получила команду
  «поставить» (`CMD_PLACE`, вилы вниз до пропадания нагрузки), стенд — учёт
  мест хранения, груза на вилах и режим «заклинивший привод». Три задания:
  перевозка через склад, перестановка в соседний проход, перегороженный проход
  (дальномер останавливает погрузчик за 35 см до препятствия) и отказ привода,
  при котором датчики положения молчат и ловит только сторожевой таймер.
/ Графическое приложение доросло до отладочного стенда: панель всех портов с
  подсветкой изменившихся за такт значений, кнопки «Пуск», «Пауза», «Сброс»,
  ползунок частоты тактирования, кнопка со списком заданий и сценариев из
  каталогов рядом с приложением, отрисовка мест хранения и паллет (видно, что
  груз действительно переехал) и красная подсветка значений, при которых
  работать нельзя. Подписи набираются Fira Code, как и весь курс.
/ Попытка записать свойство `G (faulted -> !moving)` показала дыру в модели:
  сторож поднимал аварию, а газ снимал исполнитель — на такт позже. Исправлено
  (газ снимает сам сторож). Само свойство в модель не попало: `taktc verify`
  абстрагирует управление и не отслеживает присваивания в `enter`, а состояния
  вложенных моделей при композиции вне его охвата — контрпример `[ Loader ]*`
  выдаётся даже на модели из трёх состояний (проверено отдельно). Отсюда
  правило: LTL в этой версии Takt применим к плоской модели со свойствами над
  именами состояний, как в `12-takt/model/recovery.takt`.

#### Остальные модули `c_fsm`: переносить нечего (17.08.2026)

Второй пункт ТД-9 — «прочие примеры, не привязанные к лекциям и не дублирующие
практикум». Разобраны все, что оставались:

| Модуль | Объём | Решение |
|---|---|---|
| `math_lang` | 587 строк | **уже перенесён**: это исходник `practices/06-lexical-analyze` — совпадают `LexerTokenType`, `TokenLPar`, `TokenRPar` и остальные имена |
| `forth` | 616 строк | не переносить: стековая машина со словарём — интерпретатор, а не система управления; темы виртуальных машин в курсе нет |
| `microc` | 786 строк | не переносить: компилятор подмножества Си; лексер уже показан лекцией 6, остальное за рамками предмета |
| `algorithms/tree.cpp` | 92 строки | не переносить: алгоритм на графах, к автоматам отношения не имеет |
| `chess` | `chess_main.c` пуст (`return 0`), вся программа — 182 строки GUI | не переносить: вне предмета курса |

Отсюда и общий вывод по репозиторию `c_fsm`: перенесено всё, что относится к
предмету, — умный муравей, прикладные регулярные выражения, сыпучие материалы
и погрузчик; остальное либо уже в практикуме, либо к автоматному
программированию не относится.

**ТД-9 закрыт.** Практика заведена, погрузчик перенесён, решение о языках и
графике записано и применено, прочие модули разобраны.

Что осталось за его границами и передано в другие задачи:

+ **переносимость графики** проверена только на macOS. Сборка и работа на
  Linux и Windows, поведение без дисплея (код `IsWindowReady()` написан, но в
  такой среде не запускался) и масштабирование на HiDPI — это работа с
  конвейером, и она переехала в ТД-3;
+ **растровые снимки в `practices/21-additional/images`** (420 КБ, пять
  скриншотов интерфейса) — свои, не заимствованные, и переснимаются самим
  приложением: `--frames N --shot ФАЙЛ`.

### ТД-10. Диаграмма Мура для каждого автомата в лекциях

Каждый пример лекций, где разбирается автоматная модель, должен сопровождаться
**диаграммой переходов** (граф Мура), оформленной по правилам курса:
`fsm-diagram` из `template/diagrams.typ`, чертёжный шрифт по ГОСТ 2.304-81
(ТД-1), начальное состояние со входящей стрелкой, заключительные — двойным
контуром.

Смысл требования простой: таблица переходов проверяется, но читается плохо;
диаграмма читается, но проверяется только глазами. Нужны обе, и они обязаны
совпадать — расхождение между рисунком и таблицей хуже отсутствия рисунка.

#### Что уже есть

| Лекция | Диаграмм | Состояние |
|---|---|---|
| `02-fsm` | 9 | основные примеры покрыты; сверить полноту |
| `03-synthesis` | 5 | разменный аппарат разобран с диаграммами |
| `04-dfa-nfa` | 3 | детерминизация и минимизация; сверить с примерами |
| `05-kleene` | 4 | шаблоны конструкции Томпсона |
| `07-automata-prog` | 2 | шлагбаум; «три способа реализации» — без диаграммы |
| `08-statecharts` | 3 | statechart стиральной машины, циклограмма |

#### Где диаграмм нет

| Лекция | Автоматы без диаграммы |
|---|---|
| `06-regexp` | учебные `10+` и `xy*` (есть таблица), прикладные ДКА: ISBN — 16 состояний, парный тег — 30, число — 8 |
| `09-verification` | модели Promela: светофор, семафор, взаимное исключение |
| `10-limits` | машина Тьюринга (есть таблица переходов), МП-автомат |
| `11-cells` | автоматы умного муравья нарисованы практикой в SVG, а не `fsm-diagram`; клеточные автоматы — вопрос отдельный |
| `12-takt` | модели `traffic`, `pump`, `watchdog`, `recovery`, `cooling` |

#### Как рисовать

+ **Малые автоматы (до 8–10 состояний)** — вручную через `fsm-diagram`: так
  рисунок остаётся частью документа, а не картинкой, и оформление берётся из
  шаблона.
+ **Автоматы, которые порождает практика** (ДКА после детерминизации,
  найденный поиском автомат муравья) — рисовать программой и вставлять как
  SVG, иначе рисунок разойдётся с моделью при первой же правке. Образец —
  `practices/11-cells/render.c` (подкоманда `svg fsm`) и вывод Graphviz в
  `04-dfa-nfa`.
+ **Большие автоматы** (парный тег — 30 состояний) — либо фрагмент с
  пояснением, либо порождённая диаграмма целиком, либо отказ от рисунка в
  пользу таблицы. Решать по читаемости на странице.

#### Согласовано с заказчиком курса (17.08.2026)

Не для всякой модели диаграмма переходов осмысленна. Решения:

+ **клеточные автоматы** (лекция 11: правила Вольфрама, «Жизнь», муравей
  Лэнгтона) — диаграмма **не нужна**. Состояние там у клетки, а не у системы,
  и граф вырождается в таблицу окрестностей, которая в лекции уже есть;
+ **машина Тьюринга** (лекция 10) — **не рисовать**. Дуга помечается тройкой
  «символ / запись / сдвиг», это уже не диаграмма Мура, а таблица переходов
  для примера `y = x + 1` нагляднее;
+ **модели Promela** (лекция 9) — **не рисовать вовсе**. Модель — композиция
  параллельных процессов, её пространство состояний порождает верификатор, и
  это принципиально другой объект;
+ **большие ДКА** (парный тег — 30 состояний, найденный поиском автомат
  муравья — 17) — **фрагмент с пояснением**. Для тега рисуется содержательная
  часть: поиск закрывающего тега с возвратами; остальное — словами и
  таблицей. Полная порождённая диаграмма остаётся там, где она уже есть
  (лекция 11), как довод о нечитаемости найденных автоматов.

Отсюда список того, что по ТД-10 предстоит нарисовать (17.08.2026 сделана
первая часть):

| Лекция | Что нарисовано | Состояние |
|---|---|---|
| `04-dfa-nfa` | ДКА `(a|b)*abb`, выписанный по смыслу, и минимальный ДКА | **сделано**: до этого у обоих были только таблицы |
| `06-regexp` | учебные `10+` и `xy*`, ДКА числа, фрагменты ДКА ISBN и парного тега | **сделано**: пять диаграмм |
| `07-automata-prog` | автомат поиска вставки (3 состояния) | **сделано**: построен по коду практики, а не по тексту |
| `08-statecharts` | автомат загрузки сыпучих материалов | **сделано**: основной цикл, 8 состояний из 11; пуск и выход в исходное опущены как линейные |
| `12-takt` | светофор, композиция «насос и сигнализация», контрпример охлаждения, модель восстановления | **сделано**: четыре диаграммы |
| `02-fsm`, `03-synthesis`, `05-kleene` | сверка полноты | **сделано**: пробелов нет, у каждой таблицы переходов есть диаграмма |

**Составные рисунки помечаются буквами (19.08.2026).** Рисунок из нескольких
кадров — лента поколений «Жизни», кадры тропы Санта-Фе, ряд диаграмм — раньше
не давал сослаться на конкретный кадр: в подписи стояло «поколения 0, 3, 6 и
7», а какой из четырёх квадратов какой, читатель считал сам. Теперь:

- кадры внутри порождённого SVG помечает сам генератор
  (`practices/11-cells/render.c`, `svg_panel_mark`): «а)», «б)», … по ГОСТ 7.32,
  кегль считается от размера клетки — рисунок вписывается в полосу набора, и
  буква фиксированного кегля становится нечитаемой тем сильнее, чем больше
  кадров;
- части, собранные в typst (`subfigures` в `template/figures.typ`), нумеруются
  тем же порядком букв автоматически;
- подпись обязана расшифровывать каждую часть: «#emph[а] --- такт 0, тропа
  целиком; #emph[б] --- такт 100, …». Это же правило применено к схеме пяти
  шаблонов Томпсона в лекции 5, где буквы были, а расшифровки не было.

**Диаграмма из полутора десятков состояний рисуется цветом, а не номерами.**
У автомата из 17 состояний (`fsm-evolved.svg`) номера всё равно не читаются,
дуги их перечёркивают. Начиная с восьми состояний вершины различаются
заливкой, номер не печатается: рисунок показывает не «какое состояние куда», а
плотность связей — ровно то, ради чего он в лекции и стоит.

**ТД-10 закрыт для существующего материала.** Правило остаётся в силе для
нового: автомат, попадающий в лекцию, приходит с диаграммой.

Замечено при работе: диаграмму из десяти и более состояний `fletcher`
раскладывает так, что длинные дуги пересекают подписи. Лечится не подбором
`bend`, а раскладкой — состояния ставятся по кругу в порядке потока, и тогда
дуги не идут через середину. Если и это не помогает, диаграмма делится:
линейное начало (пуск, инициализация) выносится в текст.

## Оформление кода на Takt — приведено 18.08.2026

Стиль задаёт сам язык (`book/src/21-code-style` в репозитории Takt), и канон
исполним командой: `taktc fmt` приводит файл к нему, `taktc fmt --check`
проверяет, не меняя. Все восемь моделей курса (`practices/12-takt/model/*.takt`
и `practices/21-additional/loader/model/loader.takt`) не соответствовали ему ни
одна; теперь соответствуют все, и расхождение ловится тестами
`12-takt-format` и `21-additional-format` в `ctest`.

Что изменилось в моделях: развёрнуты тела в одну строку (`enter { x := 1; }`),
убрано выравнивание хвостовых комментариев столбиком (канон — один пробел
перед `//`), шестнадцатеричные литералы приведены к верхнему регистру.

Два места потребовали решения, а не прогона форматтера:

- **`taktc fmt` уносит хвостовой комментарий на следующую строку** после
  объявлений `ref`, `state` и `start` — и тем меняет, к чему комментарий
  относится: в `cooling_bad.takt` пояснение «сработает на ПЕРВОМ такте»
  выезжало из состояния наружу. Поэтому у таких объявлений комментарий пишется
  **строкой выше** — так он переживает форматирование неизменным. У `var`,
  `const`, `parameter`, `import` и обычных операторов хвостовой комментарий
  остаётся на месте, и там он допустим.
- **Имена портов `BTN` и `LED`** давали предупреждение `CS-001`: канон для
  портов — `snake_case`. Переименованы в `btn` и `led`; форматтер имена не
  правит сам (это решение автора), поэтому такие предупреждения приходится
  вычитывать глазами. В лекции 12 упоминания портов исправлены вместе с кодом.

Встроенные в текст лекции фрагменты (те, что не читаются из файла) проверяются
`taktc fmt --stdin --check`; фрагмент, который не является законченной
программой, проверить так нельзя — к нему правила применяются вручную.

## Внешние инструменты

`taktc` и `takt-sim` (репозиторий [Takt](https://github.com/Pastor/Takt),
рабочая ветка `v2`; прежний адрес `BasePractice/BuT` больше не отвечает —
проверено 17.08.2026 при заведении задания CI. Локальная копия —
`/Volumes/HOME/Github/BuT`, там же документация языка в
`book/src` и примеры в `examples/`) нужны практикам `12-takt` и
`21-additional`. Каталог с ними задаётся переменной `STATECRAFT_TAKT_DIR`
(переменная окружения или CMake), по умолчанию — `~/.local/bin`, затем `PATH`.
Без `taktc` подпроект пропускается: в `12-takt` — целиком, в `21-additional` —
только система управления, планировщик и модель установки собираются всегда.
Если найден только один из двух инструментов, CMake предупреждает. Прежнее имя
переменной `STATECRAFT_TAKTC` продолжает работать.

SPIN нужен `scripts/check-promela.sh` и ищется в `PATH` (переопределяется
переменной `SPIN`); при отсутствии проверка мягко пропускается.

## Известные шероховатости репозитория

- Двоичные файлы в репозитории остались осознанно и только два вида:
  сборка SPIN под Windows (`spin.exe`, `pan.exe` — студентам без пакетного
  менеджера) и скринкаст настройки кодогенератора в `video/` (21,5 МБ).
  Остальное вычищено из истории 18.08.2026 вместе с прошлыми версиями
  (`.git`: 72 → 24 МБ) — растровые картинки лекций, чужие PDF, дампы
  компилятора и 54 бэкапа SimInTech.
- Сборка проверена только на macOS (arm64, clang) и в CI на ubuntu; ветки
  MSVC и MinGW в `StatecraftStrictC.cmake` не проверялись ни разу.
- `practices/08-fsm-control-pneumo/generated/*` — вывод SimInTech, в сборку
  не входит. С 18.08.2026 приведён в порядок: `.inc` и `.log` перекодированы
  из CP1251 в UTF-8, абсолютные пути машины сборки заменены относительными.
  Обе правки описаны в шапке `pneumo_ctrl.h`, чтобы файл не выдавался за
  нетронутый вывод генератора; копии в лекции 8 синхронизируются через
  `code-map.txt` наравне с остальными листингами.
- У `06-regular-expression` и `06-lexical-analyze` точки входа были
  заглушками (`return EXIT_SUCCESS`), и движок с лексером показывались только
  тестами. Обе дописаны: первая — 17.08.2026 (список распознавателей, прогон
  строк, печать таблицы), вторая — 18.08.2026 (поток лексем с местом каждой).
- `practices/09-promela` собственной сборки не имеет — модели проверяет
  внешний инструмент, — но с 18.08.2026 подключён к `ctest`: по тесту на
  модель, `09-promela-<имя>`. Без SPIN подпроект пропускается сообщением,
  как и `12-takt` без `taktc`.
- **`bool` в общих структурах несовместим между C90 и тестами.** Код курса
  собирается как ISO C90, где `bool` из `base_types.h` — это `int`; тесты — на
  C++, где `bool` занимает один байт. Структура, у которой `bool` стоит не
  последним полем, получает в двух трансляциях разную раскладку, и тест читает
  мусор (в `08-bulk-loading` это выглядело как SIGSEGV на ровном месте). Пока
  `base_types.h` устроен так, поля общих структур объявлять целыми.
- `practices/common/catch2/catch.hpp` — Catch2 v2.3.0 с локальной правкой
  `CATCH_TRAP` (в оригинале безусловная x86-вставка `int $3`, из-за которой
  заголовок не собирался на Apple Silicon).
- Нумерация лекций менялась 16.08.2026: клеточные автоматы выделены из
  лекции 10 в лекцию 11 (`11-cells`), Takt стал лекцией 12 (`12-takt`),
  каталог практики — `practices/12-takt`.
- Работа идёт в ветке `develop` (она же на origin); `master` — отдельная
  линия истории, в неё ничего не сливалось.
