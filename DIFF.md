# Рисунки деки: найденные дефекты и что править

Проверены все 593 слайда исходной деки и все 31 растровых рисунка.
Номера слайдов даны **по новой деке** (601 слайд, после вставки восьми схем);
в скобках — номер в исходной деке 1.8.0.

## Что проверено машинно и оказалось чистым

| Проверка | Итог |
|---|---|
| Выход фигур за габарит слайда (12192000 × 6858000 EMU) | нарушений нет |
| Выход за поля 0,72″ | нарушений нет |
| Искажение пропорций растров (placed ratio против PNG) | максимум 0,00 % — ни один рисунок не растянут |
| Наложение рисунка на подпись | нет; минимальный зазор 0,12″ на 12 слайдах, где рисунок упёрся в потолок 4,6″ |
| Пересчёт номеров слайдов | номер — поле PowerPoint, после вставки пересчитается сам |

Дальше — то, что нашлось глазами. **Правится в исходниках лекций, дека
пересобирается.** Ни один из этих рисунков в pptx не перерисовывался.

---

## 1. Текст не помещается в фигуру

| Слайд | Рисунок | Исходник | Что не так |
|---|---|---|---|
| 521 (513) | `l12-traffic.png` — «Тот же светофор диаграммой» | `lectures/src/12-takt/main.body.typ` | Подписи `красный`, `зелёный`, `жёлтый` шире своих кружков и торчат с обеих сторон контура. Самый заметный дефект деки |
| 154 (153) | `l04-min.png` — «Минимальный ДКА» | `lectures/src/04-dfa-nfa/main.body.typ` | Подпись состояния `S₀S₂` не влезает в кружок |
| 87 (86) | `l02-det-moore.png` — «Детектор как автомат Мура» | `lectures/src/02-fsm/main.body.typ` | Подпись `q₁′/1` выходит за окружность |

**Как править.** У `fsm-diagram` в `template/diagrams.typ` радиус узла постоянный.
Либо задать узлу радиус по ширине подписи, либо укоротить сами подписи
(`кр`, `зел`, `жёлт` с расшифровкой в подписи к рисунку — так уже сделано на
`l08-bulk.png`, где состояния названы `откр`, `закр`, `набор`).

## 2. Недоведённые линии и оборванные петли

Общая причина одна: у петли `fletcher` один конец не доходит до контура узла,
и петля читается как отдельная дуга, повисшая в воздухе.

| Слайд | Рисунок | Исходник | Где именно |
|---|---|---|---|
| 41 (40) | `l01-algo.png` | `01-intro` | Петля «проход» у «Закрыт» и петля «карта» у «открыт» — левый и правый концы не касаются кружка |
| 57 (56) | `l02-turnstile.png` | `02-fsm` | То же у «Закрыт» (`push`) и «открыт» (`card`) |
| 64 (63) | `l02-traffic.png` | `02-fsm` | Петли `btn` у `G_REQ`, `Y`, `RY`, `R` — по одному висящему концу у каждой |
| 236 (235) | `l06-b.png` | `06-regexp` | Петля «иначе» у состояния «тело» оторвана от узла |
| 355 (352) | `l08-bulk.png` | `08-statecharts` | Петля «вправо» проходит **сквозь** узел «возврат», а не рядом с ним |

**Как править.** В `template/diagrams.typ` у петель задать явный угол выхода и
входа (`loop-angle`) и радиус петли не меньше радиуса узла: сейчас петля
меньше узла, поэтому её дальний конец оказывается внутри контура.

## 3. Артефакты и наложения

| Слайд | Рисунок | Исходник | Что не так |
|---|---|---|---|
| 237 (236) | `l06-c.png` — декодер UTF-8 | `06-regexp` | Ребро `1 → 2` нарисовано **двусторонней** стрелкой: у перехода два наконечника. Кроме того, метка `a` набрана другим шрифтом, чем `ε`, `b`, `c`, `d` — она попала в текстовый режим вместо математического |
| 102 (101) | `l03-money.png` — диаграмма Мура разменного аппарата | `03-synthesis` | Четыре метки — `(10,3)`, `(1,0)`, `(1,1)`, `(10,4)` — стоят посреди пустого места и не привязаны ни к одной дуге; понять, какому переходу они принадлежат, нельзя |
| 280 (279) | `l07-barrier.png` — шлагбаум | `07-automata-prog` | Подписи «closed / мотор стоп, лампа гаснет» и «card / реверс» налезают друг на друга, сквозь текст проходит стрелка `CLOSING → OPENING` |
| 111 (110) | `l03-sch2.png` — автоматная схема | `03-synthesis` | Метка `q₂` лежит прямо на контуре блока `C` |
| 247 (246) | `l07-general.png` — автоматный алгоритм | `07-automata-prog` | Рамку `while (cycle)` перечёркивает линия внешнего контура: контур не разорван под меткой. Метка `X_BC/Y_BC` упирается в правую рамку |
| 355 (352) | `l08-bulk.png` | `08-statecharts` | Подписи «конец цикла» и «Заслонка закрыта» слиплись в одну строку; метка «резервуаров нет» стоит далеко от своей дуги |
| 476, 477 (468, 469) | `l11-wolfram-90/30.png` | `11-cells` | Крайние клетки нижней и верхней строк обрезаны рамкой поля: рамка проходит по клетке, а не вокруг неё |
| 510 (502) | `l11-fsm-evolved.png` — 17 состояний | `11-cells` | Номера состояний убраны совсем (ТД 1.7.0 заменил их заливкой), и состояния стали неразличимы: сослаться на конкретное состояние в тексте нельзя. Часть меток `1/Ш`, `0/П` перекрыта узлами |
| 495 (487) | `l11-highway.png` — муравей Лэнгтона | `11-cells` | Подписи частей «а) б) в) г)» набраны кеглем, который на слайде 1920 px читается как точки. По ГОСТ 7.32 части подписаны верно, но кегль надо поднять втрое |
| 443 (435) | `l10-scheme.png` — снежинка Коха | `10-limits` | Фигура занимает треть ширины кадра, остальное — пустое поле. Обрезать поля PNG при экспорте |

## 4. Разнобой, не дефект вёрстки, но заметен со слайда

- **Регистр подписей состояний.** На слайдах 41 и 57 на одной диаграмме
  соседствуют «Закрыт» и «открыт». Исходники — `01-intro`, `02-fsm`.
- **Строчная «з» в osifont.** В надписях «разблокировать», «заблокировать»,
  «Заслонка закрыта» строчная «з» рисуется знаком в рост прописной. Это
  свойство шрифта, а не ошибка вёрстки; в `CLAUDE.md` уже описан механизм
  подмены глифов (`_glyph-fix` в `template/diagrams.typ`) — при желании «з»
  добавляется в тот же список.

## 5. Тесно, но не сломано

На 12 слайдах рисунок упирается в потолок высоты 4,6″, и до подписи остаётся
0,12″ вместо обычных 0,3–0,8″: **64, 71, 102, 110, 111, 120, 145, 247, 355,
476, 477, 510**. Не наложение, но выглядит зажато. Лечится в генераторе:
уменьшить потолок до 4,4″ либо поднять подпись на 0,1″.

---

## Что уже сделано в pptx

- **Восемь новых схем** по разделу 5 требований — нативными фигурами
  PowerPoint, чёрно-белой графикой, в сетке и палитре деки. Пункт 9 (график
  ReDoS) не рисовался: замеров нет.
- **Вся дека переведена на Fira Code** — тот же шрифт, которым набраны лекции
  (`body-in-mono` в `template/theme.typ`). Calibri, Cambria и Courier New в
  деке не осталось; `majorFont`/`minorFont` темы тоже переключены.
- Кегли пересчитаны: знак Fira Code шире Calibri примерно на четверть и
  Cambria на пятую часть, при прежнем кегле строки перестали бы помещаться в
  блоки. Основной текст — 13 pt вместо 16,5 pt, лид — 11 pt, подпись —
  9,5 pt, колонтитул — 8 pt, заголовок слайда — 25 pt вместо 30 pt, номер
  лекции на разделителе — 45 pt вместо 54 pt. Это тот же приём, что в шаблоне
  лекций (`mono-body-size` 9,6 pt против 11 pt у PT Serif).
- **Палитра переведена в тёплую («Глина»)** — тёмных слайдов в деке больше
  нет: титул и разделители лекций светлые.

  | Роль | Было | Стало |
  |---|---|---|
  | Доминанта, текст | `1A2027` | `574538` |
  | Текст второго плана | `45525F` | `85705E` |
  | Показанные ранее пункты | `97A2AD` | `A8977F` |
  | Акцент | `D96A28` | `B5601F` |
  | Вспомогательный | `176B72` | `A0563F` |
  | Линии таблиц | `DCE1E6` | `EDE2D5` |
  | Фон слайда | `FFFFFF` | `FEFCF9` |
  | Фон титула и разделителей | `1A2027` | `EDE1D2` |

  Заодно поднят контраст «показанных ранее пунктов»: `97A2AD` давал к фону
  2,6 : 1 — на проекторе такие строки почти пропадали; `A8977F` даёт 3,4 : 1.
- **Fira Code нужно установить** на машине, с которой дека показывается.
  Шрифт лежит в репозитории: `lectures/fonts/`. Без него PowerPoint
  подставит свой моноширинный, и метрики поедут.
