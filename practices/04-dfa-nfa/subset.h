#ifndef STATECRAFT_SUBSET_H
#define STATECRAFT_SUBSET_H

/**
 * @file
 * Лекция 4. Два алгоритма, которыми конечный автомат приводится к
 * каноническому виду:
 *
 *   #subset_construction — детерминизация (построение по подмножествам);
 *   #dfa_minimize        — минимизация разбиением классов эквивалентности.
 *
 * Оба работают со структурами из `common/fsm.h`, поэтому вход можно
 * получить из практики лекции 5:
 *
 *     05-kleene "(a|b)*abb" | 04-dfa-nfa --minimize --dot
 *
 * Этим конвейером сверены выкладки лекции: 16 переходов ε-НКА, 5
 * подмножеств после детерминизации, 4 состояния после минимизации.
 */

#include "base_types.h"
#include "fsm.h"

#if defined(__cplusplus)
extern "C" {
#endif

/**
 * Детерминизация: построение ДКА по подмножествам состояний НКА.
 *
 * Состояние ДКА — множество состояний НКА, замкнутое по ε-переходам.
 *
 * @return false, если подмножеств оказалось больше #FSM_MAX_DFA_STATES.
 *         Предел достижим: у автомата из n состояний подмножеств 2^n, и
 *         именно этим экспоненциальным ростом детерминизация и известна.
 */
bool subset_construction(struct Dfa *dfa, const struct Nfa *nfa);

/**
 * Минимизация разбиением на классы эквивалентности.
 *
 * Классы дробятся, пока в одном классе остаются состояния, которые по
 * какому-либо символу ведут в разные классы. Недостижимые состояния
 * удаляются до начала работы: иначе они образуют собственные классы и
 * попадают в результат.
 *
 * @param out минимальный автомат
 * @param in  исходный автомат; допускается тот же объект, что и @p out
 * @return false, если результат не поместился в пределы `struct Dfa`.
 */
bool dfa_minimize(struct Dfa *out, const struct Dfa *in);

/**
 * Сколько подмножеств породила последняя детерминизация до удаления
 * недостижимых — для наблюдения за экспоненциальным взрывом.
 *
 * @note Значение глобальное и относится к последнему вызову
 * #subset_construction: это отладочный счётчик для учебных прогонов, а не
 * часть модели автомата.
 */
int subset_last_visited(void);

#if defined(__cplusplus)
}
#endif

#endif /* STATECRAFT_SUBSET_H */
