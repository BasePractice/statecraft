#ifndef STATECRAFT_THOMPSON_H
#define STATECRAFT_THOMPSON_H

/**
 * @file
 * Лекция 5. Построение ε-НКА по регулярному выражению — конструкция
 * Томпсона, то есть лемма 7 теоремы Клини, записанная кодом.
 *
 * Поддерживаемый синтаксис (ровно операции регулярных выражений из лекции,
 * без сахара PCRE):
 *
 *     a        символ алфавита (любой, кроме служебных)
 *     R S      конкатенация
 *     R|S      объединение
 *     R*       итерация Клини (ноль или более)
 *     R+       итерация (одно или более)
 *     R?       ноль или одно вхождение
 *     ( R )    группировка
 *
 * Приоритет по убыванию: постфиксные * + ?, конкатенация, объединение.
 */

#include "base_types.h"
#include "fsm.h"

#if defined(__cplusplus)
extern "C" {
#endif

/**
 * Строит по выражению ε-НКА.
 *
 * Число состояний растёт линейно: по два на каждый символ выражения, причём
 * символами считаются и `|`, и `*`. На этом свойстве в лекции 5 показано,
 * почему конструкция Томпсона не приводит к взрыву — в отличие от
 * последующей детерминизации.
 *
 * @return false, если выражение синтаксически неверно или автомат не
 *         помещается в #FSM_MAX_STATES; причина печатается в stderr.
 */
bool thompson_build(struct Nfa *nfa, const char *pattern);

#if defined(__cplusplus)
}
#endif

#endif /* STATECRAFT_THOMPSON_H */
